# TDD 사이클 메모 (minimal)

## RED
- `test_add_positive_numbers` and `test_subtract_positive_numbers` were first represented as failing placeholder checks.

## GREEN
- Implemented `add(a, b)` and `subtract(a, b)` with the smallest arithmetic behavior needed for the tests.

## REFACTOR
- Kept the implementation simple and ran the sample evaluator, pytest, ruff, and actionlint gates.
