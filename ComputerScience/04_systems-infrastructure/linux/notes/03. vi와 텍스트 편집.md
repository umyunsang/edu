---
title: 03. vi와 텍스트 편집
description: 모드 기반 편집기와 파일 수정 흐름을 이해하는 리눅스 텍스트 편집
type: lecture
tags:
- linux
- systems
course: linux
semester: 2-2
source: 리눅스-3주차.pdf
status: draft
---

> [!abstract]
> vi는 명령 기반 텍스트 편집기이며 일반 모드와 입력 모드를 구분해 편집 동작을 다룬다.

## 핵심 구조

<Tabs>
<Tab title="일반 모드">이동·명령·편집 준비</Tab>
<Tab title="입력 모드">텍스트를 실제로 삽입</Tab>
<Tab title="Vim">vi를 확장한 편집기</Tab>
</Tabs>

```mermaid
flowchart LR
 A[사용자 요청] --> B[리눅스 인터페이스]
 B --> C[운영체제 기능]
 C --> D[결과]
```

> [!important]
> 이동·명령·편집 준비·텍스트를 실제로 삽입·vi를 확장한 편집기은 실제 작업 흐름에서 연결되어야 한다.

```html preview h=135px
<div style="font-family:system-ui,sans-serif;padding:14px;border:1px solid var(--border);border-radius:12px;background:var(--card);color:var(--foreground)"><strong>03. vi와 텍스트 편집</strong><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-top:12px"><span style="padding:8px;border:1px solid var(--border);border-radius:7px">일반 모드</span><span style="padding:8px;border:1px solid var(--border);border-radius:7px">입력 모드</span><span style="padding:8px;border:1px solid var(--border);border-radius:7px">Vim</span></div></div>
```

<details>
<summary>모드 구분이 왜 중요한가?</summary>

입력과 명령을 분리해 키 입력의 의미가 상황에 따라 달라진다.
</details>

## 점검 흐름

==명령이나 설정을 외우기보다 입력·권한·실행 결과를 연결해 확인한다.==

```html preview h=125px
<div style="font-family:system-ui,sans-serif;padding:14px;border:1px solid var(--border);border-radius:12px;background:var(--card);color:var(--foreground)"><div style="display:flex;gap:9px;align-items:center"><span style="padding:8px;background:var(--chart-1);color:var(--background);border-radius:7px">입력</span><b>→</b><span style="padding:8px;background:var(--chart-2);color:var(--background);border-radius:7px">권한</span><b>→</b><span style="padding:8px;background:var(--chart-3);color:var(--background);border-radius:7px">결과</span></div></div>
```

<details><summary>실습 전 확인</summary>

작업 대상과 현재 사용자, 필요한 권한을 먼저 확인하고 결과를 기록한다.
</details>

> [!warning]
> 시스템 작업은 잘못된 대상이나 권한으로 실행하면 예상보다 큰 영향을 줄 수 있다.

다음 확인을 실제 작업 순서에 넣는다.

> [!tip]
> 작은 예시에서 먼저 실행 결과를 관찰하고, 같은 절차를 확장한다.

### 스스로 점검

- 일반 모드의 역할을 설명할 수 있는가?
- 입력 모드와 Vim가 어떤 순서로 연결되는가?
- 실행 전 무엇을 확인해야 하는가?
