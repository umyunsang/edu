---
title: 11. 파일 시스템 관리
description: 파일·디렉터리·메타데이터로 저장 장치의 정보를 논리적으로 관리하는 방법
type: lecture
tags:
- operating-systems
- systems
course: operating-systems
semester: 2-2
source: 11. 파일 시스템 관리.pdf
status: draft
---

> [!abstract]
> 파일 시스템은 응용프로그램이 저장 장치를 직접 다루지 않도록 파일과 디렉터리, 메타데이터를 통해 입출력을 관리한다.

## 핵심 구조

<Tabs>
<Tab title="파일">정보를 저장하는 논리적 단위</Tab>
<Tab title="디렉터리">파일을 계층적으로 찾는 구조</Tab>
<Tab title="연산">찾기·열기·읽기·쓰기·닫기의 운영체제 경로</Tab>
</Tabs>

```mermaid
flowchart LR
    A[요청] --> B[운영체제 판단]
    B --> C[자원 또는 상태 관리]
    C --> D[실행 결과]
```

> [!important]
> 정보를 저장하는 논리적 단위와 파일을 계층적으로 찾는 구조은 별개가 아니라, 운영체제가 실행을 관리할 때 함께 고려하는 관점이다.

```html preview h=145px
<div style="font-family:system-ui,sans-serif;padding:16px;border:1px solid var(--border);border-radius:12px;background:var(--card);color:var(--foreground)"><strong>11. 파일 시스템 관리</strong><div style="display:grid;grid-template-columns:repeat(3,1fr);gap:9px;margin-top:13px"><span style="padding:10px;border:1px solid var(--border);border-radius:8px">파일</span><span style="padding:10px;border:1px solid var(--border);border-radius:8px">디렉터리</span><span style="padding:10px;border:1px solid var(--border);border-radius:8px">연산</span></div></div>
```

<details>
<summary>파일 이름만으로 충분한가?</summary>

권한·크기·위치 같은 메타데이터가 있어야 운영체제가 저장과 접근을 관리한다.
</details>

## 판단과 검증

==정책의 이름만 외우기보다, 어떤 요청·자원·상태 변화에 적용되는지 추적하는 것이 핵심이다.==

```html preview h=145px
<div style="font-family:system-ui,sans-serif;padding:16px;border:1px solid var(--border);border-radius:12px;background:var(--card);color:var(--foreground)"><div style="display:flex;gap:10px;align-items:center"><span style="padding:10px;background:var(--chart-1);color:var(--background);border-radius:8px">관찰</span><b>→</b><span style="padding:10px;background:var(--chart-2);color:var(--background);border-radius:8px">선택</span><b>→</b><span style="padding:10px;background:var(--chart-3);color:var(--background);border-radius:8px">검증</span></div><p style="margin:12px 0 0">상태와 정책의 결과를 함께 기록한다.</p></div>
```

<details>
<summary>어떤 비교가 필요한가?</summary>

같은 입력 조건에서 정책을 바꿔 보고, 성능뿐 아니라 공정성·보호·자원 사용량의 차이를 관찰한다.
</details>

> [!warning]
> 한 가지 지표만 좋다고 전체 시스템의 선택이 자동으로 정당화되지는 않는다. 충돌하는 목표와 예외 상황을 함께 점검한다.

다음 관찰을 학습 흐름에 연결한다.

> [!tip]
> 학습 팁: 요청이 들어온 순간부터 운영체제가 바꾸는 상태를 화살표로 직접 그려 본다.

### 스스로 점검

- 파일의 역할을 한 문장으로 설명할 수 있는가?
- 디렉터리와 연산가 어떤 상황에서 연결되는가?
- 정책을 비교할 때 무엇을 측정해야 하는가?
