---
title: 06. 집적 회로
description: 논리 회로를 집적해 복잡한 기능을 구현하는 집적 회로
type: lecture
tags:
  - computer-architecture
  - hardware
course: computer-architecture
semester: 2-2
source: 6.집적 회로.pdf
status: draft
slides: true
---

> [!abstract]
> 집적 회로는 여러 전자 소자를 하나의 칩에 집적해 논리 기능을 구현하는 기술이다.

## 핵심 구조

<Tabs><Tab title="입력">명령·데이터·신호를 받는다.</Tab><Tab title="처리">구성 요소가 규칙에 따라 동작한다.</Tab><Tab title="출력">결과나 제어 신호를 다음 단계로 보낸다.</Tab></Tabs>

```mermaid
flowchart LR
 A[명령 또는 데이터] --> B[구성 요소]
 B --> C[제어와 상태]
 C --> D[결과]
```

> [!important]
> 구성 요소의 역할은 다른 구성 요소와 주고받는 데이터와 신호 속에서 읽어야 한다.

```html preview h=120px
<div style="font-family:system-ui,sans-serif;padding:14px;border:1px solid var(--border);border-radius:12px;background:var(--card);color:var(--foreground)"><strong>06. 집적 회로</strong><p>입력 → 처리 → 제어 → 결과</p></div>
```

<details><summary>왜 단계별로 보나?</summary>

단계마다 바뀌는 값과 신호를 분리하면 전체 명령 실행을 설명할 수 있다.
</details>

## 검증 관점

==같은 구성 요소도 어떤 명령과 상태에서 동작하는지 함께 확인해야 한다.==

```html preview h=110px
<div style="font-family:system-ui,sans-serif;padding:14px;border:1px solid var(--border);border-radius:12px;background:var(--card);color:var(--foreground)">관찰 <b>→</b> 실행 <b>→</b> 결과 확인</div>
```

<details><summary>작은 예시</summary>

입력 하나를 정하고, 어떤 제어 신호와 결과가 이어지는지 순서대로 기록한다.
</details>

> [!warning]
> 데이터 경로와 제어 경로를 혼동하면 명령 실행 순서를 잘못 이해할 수 있다.

다음 확인을 학습 순서에 넣는다.

> [!tip]
> 구성 요소별 입력과 출력을 도식으로 표기해 본다.

### 스스로 점검

- 이 구성 요소의 입력과 출력은 무엇인가?
- 어떤 제어 신호 또는 상태가 필요한가?
- 결과를 어떻게 검증할 수 있는가?

---

## 원본 강의 흐름 인터랙티브

```html preview
<div style="font-family:system-ui,sans-serif;padding:20px;color:var(--foreground)">
  <label for="noteFlow525474" style="font-size:14px;font-weight:600">집적 회로 단계</label>
  <div id="noteFlow525474-out" style="font-size:26px;font-weight:700;color:var(--chart-1);margin:6px 0">집적 회로 핵심 개념</div>
  <input id="noteFlow525474" type="range" min="1" max="3" step="1" value="1" style="width:100%;accent-color:var(--primary)" />
  <p style="font-size:13px;color:var(--muted-foreground)">슬라이더를 움직이면 원본 PDF에서 정리한 이 노트의 흐름을 확인합니다.</p>
  <script>
    var noteFlow525474 = document.getElementById('noteFlow525474');
    var noteFlow525474Out = document.getElementById('noteFlow525474-out');
    var noteFlow525474Steps = ["집적 회로 핵심 개념","집적 회로 처리 절차","집적 회로 검증·응용"];
    noteFlow525474.addEventListener('input', function () { noteFlow525474Out.textContent = noteFlow525474Steps[Number(noteFlow525474.value) - 1]; });
  </script>
</div>
```
