---
title: "00. big-data-analysis 강의 흐름 지도 - 분산 빅데이터 전 단원 로드맵 (Curriculum Map)"
description: "빅데이터 분석(Big Data Analysis) 교과목의 4대 핵심 학습 영역(데이터 생태계와 하둡 HDFS/MapReduce, Apache Spark 인메모리 분산 아키텍처, 통계적 추론과 변수 선택, 스트리밍 알고리즘 및 MLOps MLflow)을 인터랙티브 커리큘럼 맵으로 조망한다."
type: lecture
tags:
  - lecture
  - big-data-analysis
  - curriculum-map
  - road-map
course: big-data-analysis
semester: "3-2"
source: "sources/*.pdf"
source_pages: 629
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

본 강의 흐름 지도는 **빅데이터 분석(Big Data Analysis)** 교과목의 전 단원 강의 노트를 4대 핵심 분산 컴퓨팅 및 머신러닝 엔지니어링 스테이지로 구조화하여 제공한다.

---

## 1. 4대 핵심 분산 컴퓨팅 학습 스테이지

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- Stage 1 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-1)">Stage 1: Ecosystem & Hadoop</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 빅데이터 5V & DIKW 피라미드<br/>
        • HDFS 128MB 블록 3중 복제<br/>
        • MapReduce 4단계 분산 처리
      </div>
    </div>
    <!-- Stage 2 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">Stage 2: Apache Spark Core</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • Driver-Executor 인메모리 엔진<br/>
        • Spark Unified Stack (SQL, MLlib)<br/>
        • RDD 지연 평가 & DAG 셔플
      </div>
    </div>
    <!-- Stage 3 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-3)">Stage 3: Statistics & Selection</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 중심 극한 정리 & 가설 검정 p-value<br/>
        • 필터(ANOVA) & 래퍼(RFE) 기법<br/>
        • Lasso L1 희소성 유도 원리
      </div>
    </div>
    <!-- Stage 4 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">Stage 4: Streaming & MLOps</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 슬라이딩 윈도우 & 블룸 필터<br/>
        • 다목적 최적화 & 파레토 프론트<br/>
        • MLflow 실험 추적 & 레지스트리
      </div>
    </div>
  </div>
</div>
```

---

## 2. 인터랙티브 분산 빅데이터 커리큘럼 네비게이터

스테이지 버튼을 클릭하면 해당 단원의 세부 학습 주제와 핵심 요약이 실시간으로 전환된다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 스테이지 선택 탭 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <button id="bmap-btn-1" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">1. 하둡 생태계 (01~02)</button>
    <button id="bmap-btn-2" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">2. Apache Spark 코어 (03~04)</button>
    <button id="bmap-btn-3" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">3. 통계 & 변수 선택 (05~06)</button>
    <button id="bmap-btn-4" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">4. 스트리밍 & MLOps (07~09)</button>
  </div>

  <!-- 세부 컨테이너 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px">
    <div id="bmap-title" style="font-size:14px;font-weight:800;color:var(--primary);margin-bottom:8px">
      Stage 1: 빅데이터 생태계와 하둡 분산 시스템
    </div>
    <div id="bmap-desc" style="font-size:12px;color:var(--muted-foreground);line-height:1.6;margin-bottom:10px">
      5V 데이터 특성과 DIKW 계층, HDFS 마스터-워커 분산 파일 시스템 및 MapReduce 분산 처리 파이프라인.
    </div>
    <div id="bmap-list" style="display:flex;flex-direction:column;gap:6px"></div>
  </div>
</div>

<script>
(function() {
  var bstages = [
    {
      title: 'Stage 1: 빅데이터 생태계와 하둡 분산 시스템 (Notes 01 ~ 02)',
      desc: '5V 데이터 특성과 DIKW 계층, HDFS 128MB 블록 3중 복제 정책, MapReduce 4단계(Split-Map-Shuffle-Reduce) 분산 처리 파이프라인.',
      notes: [
        '01. 빅데이터 분석의 범위와 데이터 생태계 (5V & DIKW)',
        '02. 하둡과 분산 아키텍처 - HDFS와 MapReduce'
      ]
    },
    {
      title: 'Stage 2: Apache Spark 인메모리 분산 아키텍처 (Notes 03 ~ 04-2)',
      desc: 'Driver-Executor 런타임 모델, Spark Unified Stack(SQL, Streaming, MLlib, GraphX), RDD 5대 속성과 지연 평가, 좁은/넓은 의존성 및 셔플 메커니즘.',
      notes: [
        '03. Apache Spark 미리보기 - 인메모리 분산 컴퓨팅 아키텍처',
        '04-1. Apache Spark의 배경 - 분산 컴퓨팅의 진화와 통합 스택',
        '04-2. Spark RDD와 워크플로 - 지연 평가와 좁은/넓은 의존성'
      ]
    },
    {
      title: 'Stage 3: 통계적 가설 검정과 변수 선택 기법 (Notes 05 ~ 06)',
      desc: '중심 극한 정리(CLT), 귀무가설/대립가설 및 p-value 판정, 차원의 저주 극복을 위한 3대 변수 선택 기법(ANOVA 필터, RFE 래퍼, Lasso L1 임베디드).',
      notes: [
        '05. 데이터 통계 기초 - 가설 검정과 중심 극한 정리',
        '06. 변수 선택 - 필터·래퍼·임베디드 기법과 희소성'
      ]
    },
    {
      title: 'Stage 4: 실시간 스트리밍과 MLOps 라이프사이클 (Notes 07 ~ 09)',
      desc: '슬라이딩 윈도우, 블룸 필터(Bloom Filter)의 해시 비트맵 멤버십 검사, 다목적 최적화와 파레토 프론트(Pareto Front), MLflow 실험 추적 및 모델 레지스트리.',
      notes: [
        '07. 스트리밍 알고리즘과 확률적 데이터 구조 (Bloom Filter)',
        '08. 다목적 최적화 - 파레토 지배와 파레토 프론트',
        '09. MLflow 설치와 실행 - MLOps 실험 추적과 모델 레지스트리'
      ]
    }
  ];

  var sTitle = document.getElementById('bmap-title');
  var sDesc = document.getElementById('bmap-desc');
  var sList = document.getElementById('bmap-list');

  function setStage(idx) {
    for (var i = 1; i <= 4; i++) {
      var btn = document.getElementById('bmap-btn-' + i);
      if (i === idx + 1) {
        btn.style.background = 'var(--primary)';
        btn.style.color = '#fff';
      } else {
        btn.style.background = 'transparent';
        btn.style.color = 'var(--foreground)';
      }
    }

    var st = bstages[idx];
    sTitle.textContent = st.title;
    sDesc.textContent = st.desc;
    sList.innerHTML = '';

    st.notes.forEach(function(n) {
      var d = document.createElement('div');
      d.style.padding = '8px 12px';
      d.style.background = 'var(--card)';
      d.style.border = '1px solid var(--border)';
      d.style.borderRadius = 'var(--radius)';
      d.style.fontSize = '12px';
      d.style.fontWeight = '600';
      d.style.color = 'var(--foreground)';
      d.textContent = '📄 ' + n;
      sList.appendChild(d);
    });
  }

  for (var k = 1; k <= 4; k++) {
    (function(idx) {
      document.getElementById('bmap-btn-' + (idx + 1)).addEventListener('click', function() {
        setStage(idx);
      });
    })(k - 1);
  }

  setStage(0);
})();
</script>
```
