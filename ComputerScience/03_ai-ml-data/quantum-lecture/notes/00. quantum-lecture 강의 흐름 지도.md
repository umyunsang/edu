---
title: "00. quantum-lecture 강의 흐름 지도 - 양자 컴퓨팅 알고리즘 전 단원 로드맵 (Curriculum Map)"
description: "양자 컴퓨팅(Quantum Computing) 교과목의 4대 핵심 영역(양자 게이트 및 힐베르트 공간 기초, Grover/Shor 양자 알고리즘과 AWS Braket, 양자 화학 VQE 및 양자 머신러닝 QML, 양자 하드웨어 아키텍처와 SQD)을 인터랙티브 커리큘럼 맵으로 조망한다."
type: lecture
tags:
  - lecture
  - quantum-lecture
  - curriculum-map
  - road-map
course: quantum-lecture
semester: extracurricular
source: "sources/*.pdf"
source_pages: 350
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

본 강의 흐름 지도는 **양자 컴퓨팅과 알고리즘(Quantum Computing & Algorithms)** 교과목의 전 단원 강의 노트를 4대 핵심 학습 스테이지로 구조화하여 제공한다.

---

## 1. 4대 핵심 양자 컴퓨팅 학습 스테이지

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- Stage 1 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-1)">Stage 1: Qubit & Algebra</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 블로흐 구 & 단일/다중 큐비트 게이트<br/>
        • 힐베르트 공간 $\mathbb{C}^{2^n}$ & 텐서 곱<br/>
        • Born의 측정 공리 & 기댓값 계산
      </div>
    </div>
    <!-- Stage 2 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">Stage 2: Core Algorithms</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • Grover $O(\sqrt{N})$ 진폭 증폭 검색<br/>
        • Shor QFT 기반 소인수분해<br/>
        • AWS Braket SDK & 시뮬레이터 실습
      </div>
    </div>
    <!-- Stage 3 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-3)">Stage 3: Chemistry & QML</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • VQE 분자 바닥 에너지 변분 최적화<br/>
        • 양자 커널 $K(\mathbf{x}, \mathbf{x}')$ & QSVM<br/>
        • VQC & 파라미터 시프트 룰
      </div>
    </div>
    <!-- Stage 4 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">Stage 4: Hardware & SQD</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 4대 큐비트 플랫폼 (초전도, 이온트랩)<br/>
        • 결맞음 시간 ($T_1, T_2$) & 충실도<br/>
        • SQD 부분 공간 양자 대각화
      </div>
    </div>
  </div>
</div>
```

---

## 2. 인터랙티브 양자 컴퓨팅 커리큘럼 네비게이터

스테이지 버튼을 클릭하면 해당 단원의 세부 학습 주제와 핵심 요약이 실시간으로 전환된다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 스테이지 선택 탭 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <button id="qmap-btn-1" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">1. 큐비트 & 선형대수 (01, 03)</button>
    <button id="qmap-btn-2" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">2. 대표 양자 알고리즘 (01-1, 02)</button>
    <button id="qmap-btn-3" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">3. 양자 화학 & QML (03-1, 04)</button>
    <button id="qmap-btn-4" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">4. 하드웨어 & SQD (05, 05-1)</button>
  </div>

  <!-- 세부 컨테이너 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px">
    <div id="qmap-title" style="font-size:14px;font-weight:800;color:var(--primary);margin-bottom:8px">
      Stage 1: 큐비트 기초와 선형대수학
    </div>
    <div id="qmap-desc" style="font-size:12px;color:var(--muted-foreground);line-height:1.6;margin-bottom:10px">
      블로흐 구 좌표계, 파울리 X/Y/Z, 아다마르(H), CNOT 게이트 행렬, 힐베르트 공간 내적과 Born's Rule 측정 공리.
    </div>
    <div id="qmap-list" style="display:flex;flex-direction:column;gap:6px"></div>
  </div>
</div>

<script>
(function() {
  var qstages = [
    {
      title: 'Stage 1: 큐비트 기초와 선형대수학 (Notes 01, 03)',
      desc: '블로흐 구 좌표계, 파울리 X/Y/Z, 아다마르(H), CNOT 게이트 행렬, 힐베르트 공간 내적과 Borns Rule 측정 공리.',
      notes: [
        '01. 양자 기초 게이트 설명 - 단일/다중 큐비트 연산과 블로흐 구',
        '03. 양자컴퓨팅을 위한 물리 및 선형대수 - 힐베르트 공간과 텐서곱'
      ]
    },
    {
      title: 'Stage 2: 핵심 양자 알고리즘과 AWS Braket (Notes 01-1, 02)',
      desc: 'Grover의 O(√N) 진폭 증폭 데이터베이스 검색, Shor의 QFT 기반 다항 시간 소인수분해, AWS Braket 클라우드 SDK 실습.',
      notes: [
        '01-1. 양자 알고리즘 소개 - Grover 검색과 Shor 소인수분해',
        '02. 양자클라우드 Braket 기초 사용법 - AWS Braket SDK와 양자 회로'
      ]
    },
    {
      title: 'Stage 3: 양자 화학과 양자 머신러닝 (Notes 03-1, 04)',
      desc: 'VQE 분자 바닥 에너지 변분 최적화, 조던-위그너 변환, 양자 커널 매핑 K(x, x), QSVM 분류기, VQC 파라미터 시프트 룰.',
      notes: [
        '03-1. 화학 알고리즘 소개 - VQE와 변분 양자 고유값 계산기',
        '04. 양자 머신러닝 알고리즘 소개 - QSVM과 변분 양자 분류기'
      ]
    },
    {
      title: 'Stage 4: 양자 하드웨어와 차세대 하이브리드 SQD (Notes 05, 05-1)',
      desc: '초전도, 이온트랩, 광학, 중성원자 4대 큐비트 플랫폼 비교, T1/T2 결맞음 시간, SQD 표본 기반 부분공간 양자 대각화.',
      notes: [
        '05. 양자컴퓨팅 하드웨어에 대한 이해 - 4대 큐비트 플랫폼과 결맞음 시간',
        '05-1. 하이브리드 알고리즘 소개 - SQD와 양자 부분공간 대각화'
      ]
    }
  ];

  var sTitle = document.getElementById('qmap-title');
  var sDesc = document.getElementById('qmap-desc');
  var sList = document.getElementById('qmap-list');

  function setStage(idx) {
    for (var i = 1; i <= 4; i++) {
      var btn = document.getElementById('qmap-btn-' + i);
      if (i === idx + 1) {
        btn.style.background = 'var(--primary)';
        btn.style.color = '#fff';
      } else {
        btn.style.background = 'transparent';
        btn.style.color = 'var(--foreground)';
      }
    }

    var st = qstages[idx];
    sTitle.textContent = st.title;
    sDesc.textContent = st.desc;
    sList.innerHTML = '';

    st.notes.forEach(function(n) {
      var d = document.createElement('div');
      d.style.padding = '8px 12px';
      d.style.background = 'var(--card)';
      d.style.border = '1px solid var(--border)';
      d.style.borderRadius = 'var(--radius)';
      d.style.fontSize = '12px';
      d.style.fontWeight = '600';
      d.style.color = 'var(--foreground)';
      d.textContent = '📄 ' + n;
      sList.appendChild(d);
    });
  }

  for (var k = 1; k <= 4; k++) {
    (function(idx) {
      document.getElementById('qmap-btn-' + (idx + 1)).addEventListener('click', function() {
        setStage(idx);
      });
    })(k - 1);
  }

  setStage(0);
})();
</script>
```
