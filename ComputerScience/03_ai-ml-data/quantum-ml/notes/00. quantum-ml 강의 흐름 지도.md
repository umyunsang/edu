---
title: "00. quantum-ml 강의 흐름 지도 - 양자 머신러닝 전 단원 로드맵 (Curriculum Map)"
description: "양자 머신러닝(Quantum Machine Learning) 교과목의 4대 핵심 영역(표현력 한계와 큐비트 기초, 양자 특성 공간과 임베딩, 아다마르/벨 상태 전이와 게이트 대수, 양자 회로 설계와 VQC 최적화)을 인터랙티브 커리큘럼 맵으로 조망한다."
type: lecture
tags:
  - lecture
  - quantum-ml
  - curriculum-map
  - road-map
course: quantum-ml
semester: extracurricular
source: "sources/*.pdf"
source_pages: 350
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

본 강의 흐름 지도는 **양자 머신러닝(Quantum Machine Learning)** 교과목의 전 단원 강의 노트를 4대 핵심 학습 스테이지로 구조화하여 제공한다.

---

## 1. 4대 핵심 양자 머신러닝 학습 스테이지

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- Stage 1 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-1)">Stage 1: Foundational Limits</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 고전 표현력 한계 & 무어의 법칙<br/>
        • 3대 양자 원리 (중첩/얽힘/간섭)<br/>
        • 고전 비트 vs 양자 큐비트
      </div>
    </div>
    <!-- Stage 2 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">Stage 2: Quantum Embedding</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 각도/진폭/IQP 양자 임베딩<br/>
        • 양자 커널 $K(\mathbf{x}_i, \mathbf{x}_j)$ 사상<br/>
        • 하이브리드 QNN 파이프라인
      </div>
    </div>
    <!-- Stage 3 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-3)">Stage 3: Gates & Transitions</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 아다마르(H) 균등 중첩 생성<br/>
        • 4대 벨 상태 $|\Phi^\pm\rangle, |\Psi^\pm\rangle$<br/>
        • 단일 큐비트 3축 회전 & 범용 게이트
      </div>
    </div>
    <!-- Stage 4 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">Stage 4: Circuits & VQC</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 양자 회로 깊이 & 충실도 분석<br/>
        • PQC 파라미터 시프트 룰 역전파<br/>
        • Barren Plateaus 지수 소실 극복
      </div>
    </div>
  </div>
</div>
```

---

## 2. 인터랙티브 양자 머신러닝 커리큘럼 네비게이터

스테이지 버튼을 클릭하면 해당 단원의 세부 학습 주제와 핵심 요약이 실시간으로 전환된다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 스테이지 선택 탭 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <button id="qmlmap-btn-1" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">1. 표현력 & 큐비트 (01-03)</button>
    <button id="qmlmap-btn-2" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">2. 특성 공간 & 임베딩 (04-05)</button>
    <button id="qmlmap-btn-3" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">3. 게이트 & 상태전이 (06-08)</button>
    <button id="qmlmap-btn-4" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">4. 회로 & VQC (09-10)</button>
  </div>

  <!-- 세부 컨테이너 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px">
    <div id="qmlmap-title" style="font-size:14px;font-weight:800;color:var(--primary);margin-bottom:8px">
      Stage 1: 표현력의 한계와 큐비트 기초
    </div>
    <div id="qmlmap-desc" style="font-size:12px;color:var(--muted-foreground);line-height:1.6;margin-bottom:10px">
      고전 모델의 비선형 표현력 한계, 차원의 저주, 3대 양자 원리(중첩/얽힘/간섭), 비트와 큐비트의 차이.
    </div>
    <div id="qmlmap-list" style="display:flex;flex-direction:column;gap:6px"></div>
  </div>
</div>

<script>
(function() {
  var stages = [
    {
      title: 'Stage 1: 표현력의 한계와 큐비트 기초 (Notes 01-03)',
      desc: '고전 모델의 비선형 표현력 한계, 차원의 저주, 3대 양자 원리(중첩/얽힘/간섭), 비트와 큐비트의 차이.',
      notes: [
        '01. 표현력의 한계 - 고전 모델의 한계와 고차원 사상',
        '02. 왜 양자 컴퓨팅인가 - 무어의 법칙 한계와 3대 양자 원리',
        '03. Bit와 Qubit - 이진 비트와 양자 큐비트의 물리적 차이'
      ]
    },
    {
      title: 'Stage 2: 양자 특성 공간과 임베딩 (Notes 04-05)',
      desc: '고전 데이터의 양자화 기법(각도/진폭/IQP 임베딩), 양자 커널 추정기, 하이브리드 양자-고전 신경망.',
      notes: [
        '04. Quantum Feature Space - 양자 특성 공간과 3대 데이터 임베딩',
        '05. QML에서 Quantum의 역할 - 양자 가속과 하이브리드 QNN'
      ]
    },
    {
      title: 'Stage 3: 게이트 대수와 상태 전이 분석 (Notes 06-08)',
      desc: '아다마르(H) 중첩 생성, CNOT 제어 게이트와 4대 벨 상태 얽힘, 단일 큐비트 3축 회전 및 범용 게이트 셋.',
      notes: [
        '06. Hadamard Gate - 양자 중첩 생성과 기저 변환',
        '07. 상태변화 분석 - 2-큐비트 얽힘과 벨 상태 생성',
        '08. Quantum Gate 개념 - 단일 큐비트 회전과 범용 게이트 셋'
      ]
    },
    {
      title: 'Stage 4: 양자 회로 설계와 VQC 최적화 (Notes 09-10 & 과정 요약)',
      desc: '양자 회로 깊이와 충실도 합성, 파라미터 시프트 룰 역전파, Barren Plateaus 지수 소실 극복 전략.',
      notes: [
        '09. Quantum Circuit - 양자 회로 설계와 합성 복잡도',
        '10. Quantum Circuit과 QML - 파라미터화 회로와 Barren Plateaus 현상',
        '양자 ML 과정 - 160시간 양자 머신러닝 집중 과정 종합 정리'
      ]
    }
  ];

  var sTitle = document.getElementById('qmlmap-title');
  var sDesc = document.getElementById('qmlmap-desc');
  var sList = document.getElementById('qmlmap-list');

  function setStage(idx) {
    for (var i = 1; i <= 4; i++) {
      var btn = document.getElementById('qmlmap-btn-' + i);
      if (i === idx + 1) {
        btn.style.background = 'var(--primary)';
        btn.style.color = '#fff';
      } else {
        btn.style.background = 'transparent';
        btn.style.color = 'var(--foreground)';
      }
    }

    var st = stages[idx];
    sTitle.textContent = st.title;
    sDesc.textContent = st.desc;
    sList.innerHTML = '';

    st.notes.forEach(function(n) {
      var d = document.createElement('div');
      d.style.padding = '8px 12px';
      d.style.background = 'var(--card)';
      d.style.border = '1px solid var(--border)';
      d.style.borderRadius = 'var(--radius)';
      d.style.fontSize = '12px';
      d.style.fontWeight = '600';
      d.style.color = 'var(--foreground)';
      d.textContent = '📄 ' + n;
      sList.appendChild(d);
    });
  }

  for (var k = 1; k <= 4; k++) {
    (function(idx) {
      document.getElementById('qmlmap-btn-' + (idx + 1)).addEventListener('click', function() {
        setStage(idx);
      });
    })(k - 1);
  }

  setStage(0);
})();
</script>
```
