---
title: "computer-vision 강의 흐름 지도"
description: "컴퓨터 비전 6개 대단원(개요, 2D 영상 처리, 코너·품질, 특징 매칭·RANSAC, 스테레오 비전·깊이, 3D 기하·카메라 캘리브레이션) 및 중간·기말고사 대비 핵심 연계 로드맵을 인터랙티브 커리큘럼 맵으로 제공한다."
type: lecture
tags:
  - lecture
  - computer-vision
course: computer-vision
semester: "4-1"
source: "sources/*.pdf"
source_pages: 606
status: stable
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

컴퓨터 비전 전공 과정은 2차원 픽셀 신호 처리에서 출발하여 특징 매칭과 투영 기하학, 그리고 3차원 공간 복원 및 파운데이션 깊이 추정 모델까지 체계적으로 연결된다.

---

## 1. 전공 커리큘럼 연계 맵 (Interactive Course Map)

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 단계 탭 버튼 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <button id="cvmap-b1" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-weight:600;font-size:11px;cursor:pointer">1. 개요 & 2D</button>
    <button id="cvmap-b2" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-weight:600;font-size:11px;cursor:pointer">2. 코너 & 분할</button>
    <button id="cvmap-b3" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-weight:600;font-size:11px;cursor:pointer">3. 호모그래피 & RANSAC</button>
    <button id="cvmap-b4" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-weight:600;font-size:11px;cursor:pointer">4. 스테레오 비전</button>
    <button id="cvmap-b5" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-weight:600;font-size:11px;cursor:pointer">5. 3D & 캘리브레이션</button>
  </div>

  <!-- 단계 상세 설명 패널 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px">
    <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:6px">
      <span id="cvmap-title" style="font-size:14px;font-weight:800;color:var(--primary)">1단계: 컴퓨터 비전 개요 및 2D 영상 처리</span>
      <span id="cvmap-files" style="font-size:11px;font-weight:700;color:var(--chart-1)">01, 02번 노트</span>
    </div>
    <div id="cvmap-desc" style="font-size:12px;line-height:1.6;color:var(--muted-foreground);margin-bottom:10px">
      역문제(Inverse Problem)의 본질을 이해하고, 감마 보정(γ ≈ 0.45/2.2), 모폴로지 연산(팽창·침식), 동차좌표계 2D 아핀 변환(T, R, S) 및 후방 사상(Backward Mapping)을 학습합니다.
    </div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;font-size:11px">
      <div style="padding:6px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
        <span style="color:var(--muted-foreground)">핵심 수식:</span>
        <div id="cvmap-eq" style="font-weight:700;color:var(--chart-2);margin-top:2px">I_out = 255·(I_in/255)^γ</div>
      </div>
      <div style="padding:6px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
        <span style="color:var(--muted-foreground)">평가/실습 포인트:</span>
        <div id="cvmap-prac" style="font-weight:700;color:var(--chart-3);margin-top:2px">OpenCV BGR 채널 슬라이싱, LUT 감마 보정</div>
      </div>
    </div>
  </div>
</div>

<script>
(function() {
  var stages = [
    {
      title: '1단계: 컴퓨터 비전 개요 및 2D 영상 처리',
      files: '01, 02번 노트',
      desc: '역문제(Inverse Problem)의 본질을 이해하고, 감마 보정(γ ≈ 0.45/2.2), 모폴로지 연산(팽창·침식), 동차좌표계 2D 아핀 변환(T, R, S) 및 후방 사상(Backward Mapping)을 학습합니다.',
      eq: 'I_out = 255·(I_in/255)^γ,  X\' = M X',
      prac: 'OpenCV BGR 채널 슬라이싱, cv2.LUT, cv2.warpAffine'
    },
    {
      title: '2단계: 코너 검출, 영역 분할과 영상 품질',
      files: '03번 노트',
      desc: '해리스 코너 검출기의 구조 텐서 M 유도, 고유값 해석(λ₁, λ₂), 응답 점수 R 계산, Shi-Tomasi 검출기, 오츠(Otsu) 최적 이진화, PSNR/SSIM 지표를 다룹니다.',
      eq: 'R = det(M) - k·(tr(M))²,  σ_B²(t) 최대화',
      prac: 'cv2.cornerHarris, cv2.goodFeaturesToTrack, cv2.threshold(OTSU)'
    },
    {
      title: '3단계: 특징 매칭, 호모그래피와 RANSAC',
      files: '04번 노트',
      desc: '평면 호모그래피(3×3 행렬 H, 8 DOF), DLT 최소 4점 정합 선형 방정식 유도, 최소자승법의 이상치 취약성 극복을 위한 RANSAC 알고리즘을 학습합니다.',
      eq: 'b × (Ha) = 0,  ||b_i - H_cand·a_i|| < t',
      prac: 'cv2.ORB_create, cv2.BFMatcher, cv2.findHomography(cv2.RANSAC)'
    },
    {
      title: '4단계: 스테레오 비전과 깊이 추정',
      files: '05번 노트',
      desc: '기선거리(B)와 초점거리(f) 기반 삼각측량 깊이 공식(Z = fB/d), 에피폴라 기하(기초 행렬 F, 본질 행렬 E), 평행화(Rectification), 블록 매칭 및 RAFT-Stereo를 다룹니다.',
      eq: 'Z = (f·B) / d,  x\'^T F x = 0',
      prac: 'cv2.StereoSGBM, Epipolar Line 가시화, Disparity Map 생성'
    },
    {
      title: '5단계: 3D 기하와 카메라 캘리브레이션',
      files: '06번 노트',
      desc: '내부 파라미터 K(초점거리, 주점, 비대칭도), 외부 파라미터 [R|t], 3×4 투영 행렬 P, 렌즈 방사/접선 왜곡 모델 및 장(Zhang) 체커보드 캘리브레이션을 완성합니다.',
      eq: 'p ~ K [R | t] X_w,  x_corr = x(1 + k₁r² + k₂r⁴)',
      prac: 'cv2.findChessboardCorners, cv2.calibrateCamera, cv2.undistort'
    }
  ];

  var cur = 0;
  var btns = [
    document.getElementById('cvmap-b1'),
    document.getElementById('cvmap-b2'),
    document.getElementById('cvmap-b3'),
    document.getElementById('cvmap-b4'),
    document.getElementById('cvmap-b5')
  ];

  var titleEl = document.getElementById('cvmap-title');
  var filesEl = document.getElementById('cvmap-files');
  var descEl = document.getElementById('cvmap-desc');
  var eqEl = document.getElementById('cvmap-eq');
  var pracEl = document.getElementById('cvmap-prac');

  function render() {
    var s = stages[cur];
    titleEl.textContent = s.title;
    filesEl.textContent = s.files;
    descEl.textContent = s.desc;
    eqEl.textContent = s.eq;
    pracEl.textContent = s.prac;

    btns.forEach(function(b, i) {
      if (i === cur) {
        b.style.background = 'var(--primary)';
        b.style.color = '#fff';
      } else {
        b.style.background = 'transparent';
        b.style.color = 'var(--foreground)';
      }
    });
  }

  btns.forEach(function(btn, idx) {
    btn.addEventListener('click', function() {
      cur = idx;
      render();
    });
  });

  render();
})();
</script>

---

## 2. 대단원별 원본 자료 및 핵심 평가 요점

| 대단원 | 핵심 이론 및 알고리즘 | 평가 및 시험 핵심 요점 |
| :--- | :--- | :--- |
| **01. 개요 & 2D 영상** | 역문제, 감마 보정, 모폴로지, 2D 아핀 변환 | 감마 $\gamma \approx 1/2.2$ 상쇄 계산, 팽창/침식 차이, 전방 vs 후방 사상 |
| **02. 코너 & 품질** | 해리스 코너 구조 텐서 $M$, $R$ 점수, Otsu 이진화 | $R = \det(M) - k(\text{tr}(M))^2$ 부호 판별, PSNR 계산 |
| **03. 호모그래피** | $3 \times 3$ 호모그래피, 8 DOF, DLT, RANSAC 4단계 | 최소 4점 필요 이유, 최소자승법 대비 RANSAC의 이상치 내성 |
| **04. 스테레오 비전** | 삼각측량 $Z = fB/d$, 에피폴라 선 제약식, 평행화 | 시차 $d$와 깊이 $Z$의 반비례 관계, 평행화를 통한 1D 탐색 단순화 |
| **05. 3D 기하** | 내부 행렬 $K$, 외부 $[R \mid t]$, 투영 행렬 $P$, 왜곡 모델 | 내부/외부 파라미터 구분, 체커보드 캘리브레이션 4단계 |
```

