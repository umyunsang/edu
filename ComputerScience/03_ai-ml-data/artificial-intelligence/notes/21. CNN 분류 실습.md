---
title: "21. CNN 분류 실습 - PyTorch 기반 영상 분류 파이프라인 (End-to-End Image Classification)"
description: "PyTorch를 이용한 CNN 영상 분류 전체 파이프라인(데이터셋 로드, 미니배치 구성, 순전파, 손실 계산, 역전파, 검증 정확도 평가) 구현과 인터랙티브 CNN 학습 수렴 시뮬레이터를 실습한다."
type: lecture
tags:
  - lecture
  - artificial-intelligence
  - cnn
  - pytorch
  - training-loop
  - image-classification
course: artificial-intelligence
semester: "3-1"
source: "CNN_1.pdf; CNN_2.pdf"
source_pages: 45
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

합성곱 신경망을 이용한 영상 분류 실습은 **데이터 준비(전처리 및 증강)** $\to$ **모델 정의** $\to$ **손실 함수 및 옵티마이저 설정** $\to$ **에포크 기반 학습 및 검증 루프**의 4단계 표준 파이프라인으로 구성된다.

---

## 1. PyTorch 기반 표준 훈련 및 평가 루프 완전 구현

```python
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import DataLoader
from torchvision import datasets, transforms

# 1. 디바이스 설정 및 데이터 변환 정의
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.4914, 0.4822, 0.4465), (0.2470, 0.2435, 0.2616))
])

# 2. 모델, 손실 함수, 옵티마이저 선언
model = SimpleCNN(num_classes=10).to(device)
criterion = nn.CrossEntropyLoss()
optimizer = optim.Adam(model.parameters(), lr=0.001, weight_decay=1e-4)

# 3. 1 에포크 훈련 함수
def train_epoch(model, dataloader, optimizer, criterion, device):
    model.train()
    running_loss, correct, total = 0.0, 0, 0
    for images, labels in dataloader:
        images, labels = images.to(device), labels.to(device)
        
        optimizer.zero_grad()           # 기울기 초기화
        outputs = model(images)         # 순전파
        loss = criterion(outputs, labels) # 손실 계산
        loss.backward()                 # 역전파
        optimizer.step()                # 가중치 갱신
        
        running_loss += loss.item() * images.size(0)
        _, predicted = outputs.max(1)
        total += labels.size(0)
        correct += predicted.eq(labels).sum().item()
        
    return running_loss / total, 100.0 * correct / total

# 4. 검증 및 평가 함수
@torch.no_grad()
def evaluate(model, dataloader, criterion, device):
    model.eval()
    running_loss, correct, total = 0.0, 0, 0
    for images, labels in dataloader:
        images, labels = images.to(device), labels.to(device)
        outputs = model(images)
        loss = criterion(outputs, labels)
        
        running_loss += loss.item() * images.size(0)
        _, predicted = outputs.max(1)
        total += labels.size(0)
        correct += predicted.eq(labels).sum().item()
        
    return running_loss / total, 100.0 * correct / total
```

---

## 2. 인터랙티브 CNN 학습 수렴 & 정확도 시뮬레이터

[1 에포크 학습 진행] 버튼을 누를 때마다 에포크별 손실 감소 및 검증 정확도(Validation Accuracy)의 상승 곡선을 실시간으로 관찰할 수 있다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 상단 컨트롤 버튼 바 -->
  <div style="display:flex;gap:8px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <button id="tr-btn-step" style="padding:6px 14px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-weight:700;font-size:12px;cursor:pointer">1 에포크 학습 진행 ➔</button>
    <button id="tr-btn-auto" style="padding:6px 12px;background:var(--chart-2);color:#fff;border:none;border-radius:var(--radius);font-weight:700;font-size:12px;cursor:pointer">10 에포크 자동 진행</button>
    <button id="tr-btn-reset" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--muted-foreground);border-radius:var(--radius);font-size:12px;cursor:pointer">초기화</button>
  </div>

  <!-- SVG 수렴 차트 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:10px;display:flex;justify-content:center">
    <svg id="tr-chart-svg" width="360" height="160" style="overflow:visible"></svg>
  </div>

  <!-- 상태 수치 카드 -->
  <div style="display:grid;grid-template-columns:repeat(3, 1fr);gap:8px;margin-top:12px;text-align:center">
    <div style="padding:8px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius);font-size:12px">
      <div style="color:var(--muted-foreground)">현재 에포크 (Epoch)</div>
      <div id="tr-ep-txt" style="font-size:15px;font-weight:800;color:var(--primary);margin-top:2px">Epoch 0</div>
    </div>
    <div style="padding:8px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius);font-size:12px">
      <div style="color:var(--muted-foreground)">훈련 손실 (Train Loss)</div>
      <div id="tr-loss-txt" style="font-size:15px;font-weight:800;color:var(--destructive);margin-top:2px">2.302</div>
    </div>
    <div style="padding:8px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius);font-size:12px">
      <div style="color:var(--muted-foreground)">검증 정확도 (Accuracy)</div>
      <div id="tr-acc-txt" style="font-size:15px;font-weight:800;color:var(--chart-2);margin-top:2px">10.0 %</div>
    </div>
  </div>
</div>

<script>
(function() {
  var curEpoch = 0;
  var lossHistory = [2.302];
  var accHistory = [10.0];

  var btnStep = document.getElementById('tr-btn-step');
  var btnAuto = document.getElementById('tr-btn-auto');
  var btnReset = document.getElementById('tr-btn-reset');

  var epTxt = document.getElementById('tr-ep-txt');
  var lossTxt = document.getElementById('tr-loss-txt');
  var accTxt = document.getElementById('tr-acc-txt');
  var svg = document.getElementById('tr-chart-svg');

  function stepEpoch() {
    if (curEpoch >= 20) return;
    curEpoch++;
    var tLoss = 0.25 + 2.05 / Math.pow(curEpoch + 1, 0.85);
    var vAcc = 88.5 - 78.5 / Math.pow(curEpoch + 1, 0.75);

    lossHistory.push(tLoss);
    accHistory.push(vAcc);
    render();
  }

  function render() {
    epTxt.textContent = 'Epoch ' + curEpoch;
    lossTxt.textContent = lossHistory[lossHistory.length - 1].toFixed(3);
    accTxt.textContent = accHistory[accHistory.length - 1].toFixed(1) + ' %';

    // SVG 렌더링
    svg.innerHTML = '';
    var ox = 40, oy = 135, w = 280, h = 110;

    // 축
    var ax = document.createElementNS('http://www.w3.org/2000/svg', 'line');
    ax.setAttribute('x1', ox); ax.setAttribute('y1', oy);
    ax.setAttribute('x2', ox + w + 10); ax.setAttribute('y2', oy);
    ax.setAttribute('stroke', 'var(--border)'); ax.setAttribute('stroke-width', '1.5');
    svg.appendChild(ax);

    // Loss 곡선 (빨간색)
    var lPath = '';
    for (var i = 0; i <= curEpoch; i++) {
      var px = ox + (i / 20) * w;
      var py = oy - (lossHistory[i] / 2.5) * h;
      lPath += (i === 0 ? 'M ' : ' L ') + px + ' ' + py;
    }
    var lLine = document.createElementNS('http://www.w3.org/2000/svg', 'path');
    lLine.setAttribute('d', lPath); lLine.setAttribute('fill', 'none');
    lLine.setAttribute('stroke', 'var(--destructive)'); lLine.setAttribute('stroke-width', '2');
    svg.appendChild(lLine);

    // Accuracy 곡선 (초록색)
    var aPath = '';
    for (var j = 0; j <= curEpoch; j++) {
      var axX = ox + (j / 20) * w;
      var ayY = oy - (accHistory[j] / 100) * h;
      aPath += (j === 0 ? 'M ' : ' L ') + axX + ' ' + ayY;
    }
    var aLine = document.createElementNS('http://www.w3.org/2000/svg', 'path');
    aLine.setAttribute('d', aPath); aLine.setAttribute('fill', 'none');
    aLine.setAttribute('stroke', 'var(--chart-2)'); aLine.setAttribute('stroke-width', '2');
    svg.appendChild(aLine);
  }

  btnStep.addEventListener('click', stepEpoch);
  btnAuto.addEventListener('click', function() {
    for (var k = 0; k < 10; k++) stepEpoch();
  });
  btnReset.addEventListener('click', function() {
    curEpoch = 0; lossHistory = [2.302]; accHistory = [10.0];
    render();
  });

  render();
})();
</script>
```
