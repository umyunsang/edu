---
title: "20. CNN 설계 모듈 실습 - 계층 연결과 모듈화 구조 (CNN Architectural Chaining)"
description: "Conv-BatchNorm-ReLU-MaxPool 블록의 유기적 결합, 특징 추출기(Feature Extractor)와 분류기(Classifier)의 분리 설계, 텐서 평탄화(Flatten) 연결을 인터랙티브 3단 CNN 구조 파이프라인 시뮬레이터로 실습한다."
type: lecture
tags:
  - lecture
  - artificial-intelligence
  - cnn
  - pytorch
  - sequential
  - architecture
course: artificial-intelligence
semester: "3-1"
source: "CNN_1.pdf; CNN_2.pdf; Convolutional Neural Networks.pdf"
source_pages: 42
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

실무적인 딥러닝 모델 설계에서는 개별 계층을 단순 나열하기보다, **표준 Conv Block(합성곱 + 배치 정규화 + 활성화 + 풀링)**을 모듈 단위로 정의하고 이를 `nn.Sequential`로 체이닝하여 **특징 추출기(Feature Extractor)**와 **완전연결 분류기(Classifier)**로 구조화한다.

---

## 1. 표준 Conv Block 및 2단계 네트워크 아키텍처

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);display:flex;justify-content:center">
  <svg width="360" height="130" style="overflow:visible">
    <!-- Feature Extractor 영역 -->
    <rect x="10" y="10" width="220" height="110" rx="6" fill="var(--background)" stroke="var(--chart-1)" stroke-width="1.5" stroke-dasharray="3 3"/>
    <text x="20" y="26" font-size="10" font-weight="800" fill="var(--chart-1)">1. Feature Extractor (공간 특징 추출)</text>

    <!-- Block 1 -->
    <rect x="25" y="40" width="85" height="30" rx="4" fill="var(--card)" stroke="var(--primary)" stroke-width="1.5"/>
    <text x="67" y="58" font-size="10" font-weight="700" text-anchor="middle" fill="var(--foreground)">ConvBlock 1</text>
    <text x="67" y="68" font-size="8" text-anchor="middle" fill="var(--muted-foreground)">3➔32 (32x32➔16x16)</text>

    <!-- Block 2 -->
    <rect x="130" y="40" width="85" height="30" rx="4" fill="var(--card)" stroke="var(--primary)" stroke-width="1.5"/>
    <text x="172" y="58" font-size="10" font-weight="700" text-anchor="middle" fill="var(--foreground)">ConvBlock 2</text>
    <text x="172" y="68" font-size="8" text-anchor="middle" fill="var(--muted-foreground)">32➔64 (16x16➔8x8)</text>

    <line x1="110" y1="55" x2="130" y2="55" stroke="var(--primary)" stroke-width="2"/>

    <!-- Flatten & Classifier 영역 -->
    <line x1="230" y1="55" x2="250" y2="55" stroke="var(--border)" stroke-width="2"/>

    <rect x="250" y="10" width="100" height="110" rx="6" fill="var(--background)" stroke="var(--chart-2)" stroke-width="1.5" stroke-dasharray="3 3"/>
    <text x="258" y="26" font-size="10" font-weight="800" fill="var(--chart-2)">2. Classifier (분류)</text>

    <rect x="260" y="40" width="80" height="30" rx="4" fill="var(--card)" stroke="var(--chart-2)" stroke-width="1.5"/>
    <text x="300" y="55" font-size="9" font-weight="700" text-anchor="middle" fill="var(--foreground)">Flatten</text>
    <text x="300" y="65" font-size="8" text-anchor="middle" fill="var(--muted-foreground)">64×8×8 = 4096</text>

    <rect x="260" y="80" width="80" height="30" rx="4" fill="var(--primary)" stroke="none"/>
    <text x="300" y="98" font-size="9" font-weight="800" text-anchor="middle" fill="#fff">FC (4096➔10)</text>
  </svg>
</div>
```

---

## 2. PyTorch 모듈화 클래스 구현

```python
import torch
import torch.nn as nn

class ConvBlock(nn.Module):
    def __init__(self, in_c, out_c):
        super().__init__()
        self.block = nn.Sequential(
            nn.Conv2d(in_c, out_c, kernel_size=3, padding=1, bias=False),
            nn.BatchNorm2d(out_c),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=2, stride=2)
        )

    def forward(self, x):
        return self.block(x)

class SimpleCNN(nn.Module):
    def __init__(self, num_classes=10):
        super().__init__()
        self.features = nn.Sequential(
            ConvBlock(3, 32),   # (B, 3, 32, 32) -> (B, 32, 16, 16)
            ConvBlock(32, 64),  # (B, 32, 16, 16) -> (B, 64, 8, 8)
            ConvBlock(64, 128)  # (B, 64, 8, 8) -> (B, 128, 4, 4)
        )
        self.classifier = nn.Sequential(
            nn.Flatten(),
            nn.Linear(128 * 4 * 4, 256),
            nn.ReLU(inplace=True),
            nn.Dropout(0.5),
            nn.Linear(256, num_classes)
        )

    def forward(self, x):
        x = self.features(x)
        x = self.classifier(x)
        return x
```

---

## 3. 인터랙티브 CNN 스테이지별 텐서 형상 추적기

입력 이미지 크기(32px 또는 64px)와 Conv Block 개수(1~4개)를 선택하면 특징 추출기의 단계별 텐서 크기와 분류기 입력 차원이 실시간으로 추적된다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 컨트롤 바 -->
  <div style="display:flex;gap:12px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <div style="display:flex;align-items:center;gap:6px">
      <span style="font-size:12px;font-weight:700">입력 해상도:</span>
      <select id="arch-sel-res" style="padding:4px 8px;border:1px solid var(--border);border-radius:var(--radius);background:var(--background);color:var(--foreground);font-size:12px;font-weight:700">
        <option value="32" selected>32 × 32 (CIFAR-10)</option>
        <option value="64">64 × 64 (Tiny-ImageNet)</option>
        <option value="128">128 × 128 (고해상도)</option>
      </select>
    </div>

    <div style="display:flex;align-items:center;gap:6px">
      <span style="font-size:12px;font-weight:700">ConvBlock 스테이지 수:</span>
      <select id="arch-sel-stages" style="padding:4px 8px;border:1px solid var(--border);border-radius:var(--radius);background:var(--background);color:var(--foreground);font-size:12px;font-weight:700">
        <option value="2">2 스테이지 (32 ➔ 64)</option>
        <option value="3" selected>3 스테이지 (32 ➔ 64 ➔ 128)</option>
        <option value="4">4 스테이지 (32 ➔ 64 ➔ 128 ➔ 256)</option>
      </select>
    </div>
  </div>

  <!-- 단계별 텐서 테이블 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:10px">
    <table style="width:100%;border-collapse:collapse;font-size:11px;text-align:left">
      <thead>
        <tr style="border-bottom:1px solid var(--border);color:var(--muted-foreground)">
          <th style="padding:6px">계층 / 스테이지</th>
          <th style="padding:6px">연산 구성</th>
          <th style="padding:6px">출력 텐서 형상</th>
          <th style="padding:6px">공간 면적</th>
        </tr>
      </thead>
      <tbody id="arch-table-body"></tbody>
    </table>
  </div>

  <!-- 분류기 Flatten 차원 요약 -->
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:12px;text-align:center">
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius);font-size:12px">
      <div style="color:var(--muted-foreground)">최종 Flatten 벡터 차원</div>
      <div id="arch-flat-res" style="font-size:15px;font-weight:800;color:var(--chart-1);margin-top:2px">2,048 차원</div>
    </div>
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius);font-size:12px">
      <div style="color:var(--muted-foreground)">분류기 첫 FC 가중치 수</div>
      <div id="arch-fc-res" style="font-size:15px;font-weight:800;color:var(--primary);margin-top:2px">524,288 개</div>
    </div>
  </div>
</div>

<script>
(function() {
  var selRes = document.getElementById('arch-sel-res');
  var selStages = document.getElementById('arch-sel-stages');
  var tableBody = document.getElementById('arch-table-body');
  var flatRes = document.getElementById('arch-flat-res');
  var fcRes = document.getElementById('arch-fc-res');

  var channels = [32, 64, 128, 256];

  function render() {
    var inDim = parseInt(selRes.value, 10);
    var numStages = parseInt(selStages.value, 10);

    tableBody.innerHTML = '';

    // 입력 행
    var tr0 = document.createElement('tr');
    tr0.style.borderBottom = '1px solid var(--border)';
    tr0.innerHTML = '<td style="padding:6px;font-weight:700">Input Image</td><td style="padding:6px">RGB Tensor</td><td style="padding:6px;color:var(--primary);font-weight:700">(3, ' + inDim + ', ' + inDim + ')</td><td style="padding:6px">' + (inDim*inDim) + ' px</td>';
    tableBody.appendChild(tr0);

    var curDim = inDim;
    var curC = 3;

    for (var i = 0; i < numStages; i++) {
      var outC = channels[i];
      curDim = Math.floor(curDim / 2); // MaxPool 2x2

      var tr = document.createElement('tr');
      tr.style.borderBottom = '1px solid var(--border)';
      tr.innerHTML = '<td style="padding:6px;font-weight:700">Stage ' + (i + 1) + '</td><td style="padding:6px">Conv3x3 + BN + ReLU + Pool2x2</td><td style="padding:6px;color:var(--chart-2);font-weight:700">(' + outC + ', ' + curDim + ', ' + curDim + ')</td><td style="padding:6px">' + (curDim*curDim) + ' px</td>';
      tableBody.appendChild(tr);

      curC = outC;
    }

    var totalFlat = curC * curDim * curDim;
    var fc1Params = totalFlat * 256;

    flatRes.textContent = totalFlat.toLocaleString() + ' 차원';
    fcRes.textContent = fc1Params.toLocaleString() + ' 개';
  }

  selRes.addEventListener('change', render);
  selStages.addEventListener('change', render);

  render();
})();
</script>
```
