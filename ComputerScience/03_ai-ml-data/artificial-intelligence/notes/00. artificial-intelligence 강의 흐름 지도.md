---
title: "00. artificial-intelligence 강의 흐름 지도 - 전 단원 로드맵 (Curriculum Map)"
description: "인공지능(Artificial Intelligence) 정규 강의 29개 주제의 체계적 학습 로드맵: 퍼셉트론 기초부터 다층 신경망(MLP), 최적화(Optimizer), 오차역전파법(Backpropagation), 합성곱 신경망(CNN) 및 고급 비전 아키텍처(AlexNet, VGGNet)까지 인터랙티브 커리큘럼 맵으로 조망한다."
type: lecture
tags:
  - lecture
  - artificial-intelligence
  - curriculum-map
  - road-map
course: artificial-intelligence
semester: "3-1"
source: "sources/*.pdf"
source_pages: 1064
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

본 강의 흐름 지도는 **인공지능(Artificial Intelligence)** 교과목의 전체 29개 강의 노트를 5대 핵심 학습 스테이지로 분류하고, 이론과 실습의 유기적 연결 고리를 제공한다.

---

## 1. 5대 핵심 학습 스테이지 로드맵

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- Stage 1 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-1)">Stage 1: Perceptron</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 단일 뉴런 수학적 모델<br/>
        • 의사결정 초평면 유도<br/>
        • 논리 게이트 및 한계
      </div>
    </div>
    <!-- Stage 2 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">Stage 2: MLP & Training</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • FC 계층 텐서 연산<br/>
        • PyTorch nn.Module 설계<br/>
        • MNIST 훈련 및 평가
      </div>
    </div>
    <!-- Stage 3 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-3)">Stage 3: Optimization</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 데이터 정규화 & 경사하강<br/>
        • 모멘텀 & Adam 옵티마이저<br/>
        • 과적합 진단 및 규제 기법
      </div>
    </div>
    <!-- Stage 4 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-4)">Stage 4: Backpropagation</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 다변수 연쇄 법칙 유도<br/>
        • 델타 오차 행렬 갱신<br/>
        • 기울기 소실과 완화
      </div>
    </div>
    <!-- Stage 5 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">Stage 5: CNN & Vision</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 합성곱·풀링 텐서 연산<br/>
        • AlexNet & VGGNet 구조<br/>
        • CIFAR-10 분류 & 아바타
      </div>
    </div>
  </div>
</div>
```

---

## 2. 인터랙티브 전 단원 인터랙티브 네비게이터

아래 스테이지 버튼을 클릭하면 해당 단원의 세부 강의 노트 목록과 학습 핵심 요약이 실시간으로 전환된다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 스테이지 선택 탭 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <button id="map-btn-1" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">1. 퍼셉트론 (01~03)</button>
    <button id="map-btn-2" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">2. 다층 신경망 MLP (04~06)</button>
    <button id="map-btn-3" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">3. 최적화 & 규제 (07~11)</button>
    <button id="map-btn-4" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">4. 역전파 & 기울기소실 (12~15)</button>
    <button id="map-btn-5" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">5. CNN & 심층 비전 (16~29)</button>
  </div>

  <!-- 세부 노트 목록 컨테이너 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px">
    <div id="map-stage-title" style="font-size:14px;font-weight:800;color:var(--primary);margin-bottom:8px">
      Stage 1: 퍼셉트론 기초와 선형 분리성
    </div>
    <div id="map-stage-desc" style="font-size:12px;color:var(--muted-foreground);line-height:1.6;margin-bottom:10px">
      인공 뉴런의 구조적 탄생부터 활성화 함수, 의사결정 경계선 도출, 그리고 Colab 실습을 통한 선형 분리 한계 검증.
    </div>
    <div id="map-stage-list" style="display:flex;flex-direction:column;gap:6px"></div>
  </div>
</div>

<script>
(function() {
  var stages = [
    {
      title: 'Stage 1: 퍼셉트론 기초와 선형 분리성 (Notes 01 ~ 03)',
      desc: '기호주의 vs 연결주의의 역사, 단일 뉴런 수학적 모델링, Sigmoid 활성화와 이진 교차 엔트로피 손실, 논리 게이트(AND, OR, NAND, XOR) 실습.',
      notes: [
        '01. Perceptron 이론 - 신경망 구성과 의사결정 경계',
        '02. Perceptron 이론 - 활성화 함수와 손실 최적화',
        '03. Perceptron 논리 게이트 실습과 선형 분리성'
      ]
    },
    {
      title: 'Stage 2: 다층 퍼셉트론(MLP)과 PyTorch 실습 (Notes 04 ~ 06)',
      desc: '은닉 계층의 다층 표현력, FC Layer 텐서 연산, PyTorch nn.Module 클래스 설계, MNIST 데이터셋 훈련 및 평가 루프 완전 구현.',
      notes: [
        '04. MLP 이론 - 다층 퍼셉트론과 전결합 계층',
        '05. MLP 실습 - MNIST 데이터셋과 PyTorch 모델 구성',
        '06. MLP 실습 - 훈련 루프와 평가 파이프라인'
      ]
    },
    {
      title: 'Stage 3: 손실 곡면 최적화와 과적합 제어 (Notes 07 ~ 11)',
      desc: '데이터 정규화와 손실 곡면 왜곡 보정, 모멘텀/NAG, AdaGrad/RMSProp/Adam 적응형 알고리즘, 편향-분산 트레이드오프, L1/L2 가중치 감쇠 및 드롭아웃.',
      notes: [
        '07. Optimization 이론 - 데이터 정규화와 경사하강법',
        '08. Optimizer - 관성과 진동 억제의 모멘텀',
        '09. Optimizer - 적응적 학습률 알고리즘 (AdaGrad, RMSProp, Adam)',
        '10. Overfitting - 과적합 진단과 편향-분산 트레이드오프',
        '11. Overfitting - 규제 기법과 드롭아웃·조기 종료'
      ]
    },
    {
      title: 'Stage 4: 오차역전파법과 기울기 소실 극복 (Notes 12 ~ 15)',
      desc: '다변수 합성함수의 연쇄 법칙, 델타 규칙(Delta Rule) 행렬 미분 유도, Sigmoid 도함수 0.25 상한에 따른 지수적 소실 증명, ReLU 및 He 초기화/ResNet 완화 기법.',
      notes: [
        '12. Backpropagation - 연쇄 법칙과 계산 그래프',
        '13. Backpropagation - 다층 신경망 가중치 역전파와 델타 오차',
        '14. Vanishing Gradient Effect - 기울기 소실 원인과 시그모이드의 한계',
        '15. Vanishing Gradient 완화 - 활성화 함수·초기화·잔차 연결'
      ]
    },
    {
      title: 'Stage 5: 합성곱 신경망(CNN) 및 심층 비전 아키텍처 (Notes 16 ~ 29)',
      desc: '2D 교차 상관 연산, 출력 해상도 및 3D 텐서 차원 공식, 풀링/BatchNorm/Dropout 모듈, AlexNet 8계층/LRN, VGGNet 3x3 적층 수용영역 증명, CIFAR-10 실습, 필기 과제 최소자승법/Softmax 편미분 유도.',
      notes: [
        '16. CNN의 합성곱 원리 - 교차 상관 연산과 특징 맵',
        '17. CNN의 공간 크기와 채널 - 패딩, 스트라이드, 3D 텐서',
        '18. CNN 주요 설계 모듈 이론 - 풀링, 배치 정규화, 드롭아웃',
        '19. CNN 설계 모듈 실습 - Conv2d 매개변수와 텐서 차원',
        '20. CNN 설계 모듈 실습 - 계층 연결과 모듈화 구조',
        '21. CNN 분류 실습 - PyTorch 기반 영상 분류 파이프라인',
        '22. CNN Backpropagation - AlexNet 8계층 아키텍처',
        '23. CNN Backpropagation - AlexNet 정규화와 일반화 기법',
        '24. VGGNet 실습 - 3x3 합성곱 적층과 수용 영역',
        '25. CIFAR-10 분류 실습 - 심층 CNN 구현과 성능 최적화',
        '26. AI 아바타 만들기 실습 - 신경 스타일 전이와 얼굴 랜드마크',
        '27. 중간고사 - MLP 기반 CIFAR-10 분류 프로젝트 분석',
        '28. 선형대수·미적분 기초와 최소자승법 (필기 과제 1 심층 풀이)',
        '29. 다변수 미분과 손실 함수·역전파 해석 (필기 과제 2 심층 풀이)'
      ]
    }
  ];

  var sTitle = document.getElementById('map-stage-title');
  var sDesc = document.getElementById('map-stage-desc');
  var sList = document.getElementById('map-stage-list');

  function setStage(idx) {
    for (var i = 1; i <= 5; i++) {
      var btn = document.getElementById('map-btn-' + i);
      if (i === idx + 1) {
        btn.style.background = 'var(--primary)';
        btn.style.color = '#fff';
      } else {
        btn.style.background = 'transparent';
        btn.style.color = 'var(--foreground)';
      }
    }

    var st = stages[idx];
    sTitle.textContent = st.title;
    sDesc.textContent = st.desc;
    sList.innerHTML = '';

    st.notes.forEach(function(n) {
      var d = document.createElement('div');
      d.style.padding = '8px 12px';
      d.style.background = 'var(--card)';
      d.style.border = '1px solid var(--border)';
      d.style.borderRadius = 'var(--radius)';
      d.style.fontSize = '12px';
      d.style.fontWeight = '600';
      d.style.color = 'var(--foreground)';
      d.textContent = '📄 ' + n;
      sList.appendChild(d);
    });
  }

  for (var k = 1; k <= 5; k++) {
    (function(idx) {
      document.getElementById('map-btn-' + (idx + 1)).addEventListener('click', function() {
        setStage(idx);
      });
    })(k - 1);
  }

  setStage(0);
})();
</script>
```
