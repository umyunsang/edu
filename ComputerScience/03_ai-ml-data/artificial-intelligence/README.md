---
title: 인공지능
description: "동아대 컴퓨터AI공학부 2024년 1학기 인공지능 강의 덱을 원본 PDF만을 근거로 재작성한 정리문서 스물다섯 편. 세 회차가 중복을 걷어낸 실제 19편 약 625쪽을 전부 읽었다. 퍼셉트론에서 CNN 역전파와 중간고사, 마지막 생성형 실습까지 이어진다."
type: course-index
tags:
  - course
  - artificial-intelligence
  - deep-learning
  - perceptron
  - neural-network
course: artificial-intelligence
semester: "2024-1"
status: stable
created: "2026-09-22"
updated: "2026-09-25"
slides: true
---

> [!success] 과목 전체를 읽었다
> `sources/` 의 PDF 29편은 파일명이 `… 2.pdf` 로 끝나는 **중복 열 편**(바이트 크기가 원본과 같다)을 걷어내면 **실제 19편(약 625쪽)** 이다. 세 회차가 그것을 전부 읽었다 — 2026-09-22 회차가 셋(92쪽), 2026-09-23 회차가 일곱(258쪽), 2026-09-25 회차가 남은 아홉(275쪽). 마지막 회차에서 `VGGNet (실습).pdf` 와 `CNN 주요설계모듈(실습).pdf` 가 **같은 덱의 두 판본**임이 드러나, 실제로 서로 다른 강의 자료는 18편이다.

정리문서 스물네 편이 그 18편을 나눠 맡는다. 아래는 원본 진행 순서다.

> [!abstract] 이 과목의 원본에 대해
> 모든 덱의 저자가 같다 — `Dongsan Jun (dsjun@dau.ac.kr), Image Signal Processing Laboratory, Dept. of AI, Dong-A University`. 그리고 **모든 덱의 두 번째 장이 같은 그림**이다(`Overall Architecture of Deep Learning`). 그 한 장이 `sources/` 의 덱 목록과 거의 그대로 대응하고, 덱이 진행될수록 꼬리표가 붙는다 — `MLP(이론)` 판에는 `BN` 과 `Vanishing Gradient (Activation Function)` 이 추가되어 있다.
>
> **텍스트 추출이 잘 작동한다.** `Perceptron (이론)` 은 61쪽 중 52쪽에서 쪽당 180자 안팎이 잡히고, `MLP(이론)` 은 23쪽 중 22쪽이 잡힌다. 예외는 `필기 과제.pdf` 하나로, 10쪽 전체가 손글씨라 추출이 의미를 이루지 않는다.
>
> **쪽 꼬리표가 총 쪽수를 넘는 덱이 여럿이다** — `Perceptron (이론)` 은 61쪽인데 꼬리표가 `/59`(마지막 두 장이 `60/59` · `61/59`), `Perceptron (실습)` 은 8쪽인데 `/7`. `MLP(이론)` 은 2번 슬라이드 하나만 `2/39` 로, 다른 덱의 두 번째 장을 가져오면서 꼬리표가 따라온 것으로 보인다. 2회차가 더 찾았다 — `Optimization(이론)` 은 21쪽인데 분모가 **셋**(`1/14` · `2/39` · `21/76`)이고 15~20번은 분자가 분모를 넘는다. `Overfitting` 은 37쪽인데 13번부터 `/12` 이고 마지막만 `37/17` 이다. `Vanishing Gradient Effect` 32번은 `Overfitting` 덱의 간지를 **`32/12` 라는 꼬리표째** 옮겨 왔다.

## 정리문서

→ **[정리문서 목록](<./notes/index.md>)**

| 순서 | 문서 | 원본 |
| --- | --- | --- |
| 01 | [빌려 온 그림이 양쪽을 다 찌른다](<./notes/01. 빌려 온 그림이 양쪽을 다 찌른다.md>) | `Perceptron (이론)` 1~17 |
| 02 | [예측을 먼저 보여 주고, 그 예측이 나오지 않는 모델을 보여 준다](<./notes/02. 예측을 먼저 보여 주고, 그 예측이 나오지 않는 모델을 보여 준다.md>) | `Perceptron (이론)` 18~40 |
| 03 | [다섯 장에 걸쳐 XOR을 쌓는 동안 이름이 두 번 바뀌고 부호가 한 번 틀린다](<./notes/03. 다섯 장에 걸쳐 XOR을 쌓는 동안 이름이 두 번 바뀌고 부호가 한 번 틀린다.md>) | `Perceptron (이론)` 41~61 · `Perceptron (실습)` |
| 04 | [목록은 길고 고르는 기준은 한 줄이다](<./notes/04. 목록은 길고 고르는 기준은 한 줄이다.md>) | `MLP (이론)` 전체 |
| 05 | [방향은 열 장, 보폭은 이름이 없다](<./notes/05. 방향은 열 장, 보폭은 이름이 없다.md>) | `Backpropagation (이론)` 1~18 |
| 06 | [곱셈 게이트의 국소 기울기는 상대방의 값이다](<./notes/06. 곱셈 게이트의 국소 기울기는 상대방의 값이다.md>) | `Backpropagation (이론)` 19~53 |
| 07 | [54번이 답을 먼저 주고, 덱은 그 답을 쓰지 않고 열여섯 장을 간다](<./notes/07. 54번이 답을 먼저 주고, 덱은 그 답을 쓰지 않고 열여섯 장을 간다.md>) | `Backpropagation (이론)` 54~85 |
| 08 | [덱이 약속만 하고 넘어간 절은 공책에 있다](<./notes/08. 덱이 약속만 하고 넘어간 절은 공책에 있다.md>) | `필기 과제` 10쪽 |
| 09 | [예순 장을 손으로 미분하고, 실습은 한 줄로 끝낸다](<./notes/09. 예순 장을 손으로 미분하고, 실습은 한 줄로 끝낸다.md>) | `MLP(실습)` 37쪽 |
| 10 | [한 줄에 붙은 마이너스가 여섯 장을 타고 내려온다](<./notes/10. 한 줄에 붙은 마이너스가 여섯 장을 타고 내려온다.md>) | `Optimization(이론)` 21쪽 |
| 11 | [여섯 장을 들여 더 좋은 옵티마이저를 소개하고, 실습 결과는 전부 더 나쁘다](<./notes/11. 여섯 장을 들여 더 좋은 옵티마이저를 소개하고, 실습 결과는 전부 더 나쁘다.md>) | `Optimizer` 19쪽 |
| 12 | [여섯 가지를 가르치고 셋을 해 본다](<./notes/12. 여섯 가지를 가르치고 셋을 해 본다.md>) | `Overfitting` 37쪽 |
| 13 | [덱은 1을 양쪽에서 배제한다](<./notes/13. 덱은 1을 양쪽에서 배제한다.md>) | `Vanishing Gradient Effect` 1~31 |
| 14 | [다섯 층을 쌓으면 손실이 ln 10 에 붙는다](<./notes/14. 다섯 층을 쌓으면 손실이 ln 10 에 붙는다.md>) | `Vanishing Gradient Effect` 32~49 |
| 15 | [같은 불평이 세 이름으로 세 번 나온다](<./notes/15. 같은 불평이 세 이름으로 세 번 나온다.md>) | `CNN (이론)` 1~20 |
| 16 | [검산되는 예제와 검산할 수 없는 예제](<./notes/16. 검산되는 예제와 검산할 수 없는 예제.md>) | `CNN (이론)` 21~45 |
| 17 | [슬라이드 10이 약속한 절약을 슬라이드 63이 취소한다](<./notes/17. 슬라이드 10이 약속한 절약을 슬라이드 63이 취소한다.md>) | `CNN (이론)` 46~74 |
| 18 | [열 장 중 여덟 장이 지난주 덱 그대로다](<./notes/18. 열 장 중 여덟 장이 지난주 덱 그대로다.md>) | `CNN 주요설계모듈(이론)` 1~20 |
| 19 | [파란 상자는 다섯이라 쓰고 표에는 넷이 있다](<./notes/19. 파란 상자는 다섯이라 쓰고 표에는 넷이 있다.md>) | `CNN 주요설계모듈(이론)` 21~36 |
| 20 | [열여섯 장과 열일곱 장 사이에서 덱의 주인이 바뀐다](<./notes/20. 열여섯 장과 열일곱 장 사이에서 덱의 주인이 바뀐다.md>) | `CNN Backpropagation(이론)` 34쪽 |
| 21 | [여섯 층은 세 에폭을 ln 10 에 앉아 있다가 빠져나온다](<./notes/21. 여섯 층은 세 에폭을 ln 10 에 앉아 있다가 빠져나온다.md>) | `VGGNet (실습)` 1~18 |
| 22 | [훈련 손실 1등과 시험 정확도 1등이 다르다](<./notes/22. 훈련 손실 1등과 시험 정확도 1등이 다르다.md>) | `VGGNet (실습)` 19~34 · `CNN 주요설계모듈(실습)` |
| 23 | [LeNet-5의 가중치 95퍼센트는 합성곱 바깥에 있다](<./notes/23. LeNet-5의 가중치 95퍼센트는 합성곱 바깥에 있다.md>) | `CNN (실습)` 27쪽 |
| 24 | [시험은 합성곱을 묻지 않는다](<./notes/24. 시험은 합성곱을 묻지 않는다.md>) | `CIFAR10 (실습)` · `24_인공지능_중간고사` · `AI 아바타 만들기 (실습)` |

## 원본 자료

중복을 걷어낸 19편. ✅ 는 원본을 근거로 읽힌 것이다.

| | 파일 | 쪽 |
| --- | --- | ---: |
| ✅ | `Perceptron (이론).pdf` | 61 |
| ✅ | `Perceptron (실습).pdf` | 8 |
| ✅ | `MLP(이론).pdf` | 23 |
| ✅ | `MLP(실습).pdf` | 37 |
| ✅ | `Backpropagation (이론).pdf` | 85 |
| ✅ | `필기 과제.pdf` — 전 쪽 손글씨 | 10 |
| ✅ | `Optimization(이론).pdf` | 21 |
| ✅ | `Optimizer.pdf` | 19 |
| ✅ | `Overfitting.pdf` | 37 |
| ✅ | `Vanishing Gradient Effect.pdf` | 49 |
| ✅ | `CNN (이론).pdf` | 74 |
| ✅ | `CNN (실습).pdf` | 27 |
| ✅ | `CNN Backpropagation(이론).pdf` | 34 |
| ✅ | `CNN 주요설계모듈(이론).pdf` | 36 |
| ✅ | `CNN 주요설계모듈(실습).pdf` — `VGGNet (실습).pdf` 의 개정판 | 33 |
| ✅ | `VGGNet (실습).pdf` | 34 |
| ✅ | `CIFAR10 (실습).pdf` | 8 |
| ✅ | `AI 아바타 만들기 (실습).pdf` | 24 |
| ✅ | `24_인공지능_중간고사.pdf` | 5 |

전체 슬라이드는 저장하지 않는다. 정리문서가 인용하는 장면만 `assets/` 에 잘라 두었다.
