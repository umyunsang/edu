---
title: "03. OpenAI API 설정과 인증 환경 - API Key 발급과 환경변수 보안 관리 (OpenAI API Setup & Security)"
description: "OpenAI 개발자 플랫폼의 API Key 발급 절차, Billing 결제 수단 등록 및 월간 사용 한도(Budget Limit) 설정, .env 환경변수 관리(python-dotenv), 그리고 LangSmith 트레이싱 환경 구성을 인터랙티브 API 보안 시뮬레이터로 심층 학습한다."
type: lecture
tags:
  - lecture
  - large-language-models
  - openai-api
  - api-key
  - billing
  - dotenv
  - langsmith
course: large-language-models
semester: extracurricular
source: "OpenAI API.pdf"
source_pages: 35
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

OpenAI API를 활용한 프로덕션 개발을 위해서는 **안전한 API Key 발급**, **크레딧 충전 및 과금 한도(Limits) 설정**, 그리고 **소스 코드 외부에서 키를 주입하는 환경변수(`.env`) 아키텍처**가 필수적이다.

---

## 1. API Key 발급 및 보안 4대 수칙

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- 1 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-1)">1. 1회 한정 표시</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        키는 발급 시점에만 화면에 표시되며 이후 재확인 불가 (즉시 안전한 곳에 저장)
      </div>
    </div>
    <!-- 2 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">2. 하드코딩 엄격 금지</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        파이썬 소스 코드 내 직접 문자열 입력 금지 ➔ `.env` 파일과 `os.getenv` 사용
      </div>
    </div>
    <!-- 3 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-3)">3. .gitignore 등록</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        GitHub 등 공개 저장소 커밋 방지를 위해 `.gitignore`에 `.env` 필수 추가
      </div>
    </div>
    <!-- 4 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">4. 월간 한도 설정</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        예상치 못한 과금을 방지하기 위해 `Set a monthly budget`을 지정하여 요청 차단
      </div>
    </div>
  </div>
</div>
```

---

## 2. `.env` 파일 설정 및 파이썬 로드 표준 코드

프로젝트 루트 디렉토리에 `.env` 파일을 생성하고 환경변수를 구성한다:

```env
# .env 파일
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=lsv2_pt_xxxxxxxxxxxxxxxxxxxxxxxx
LANGCHAIN_PROJECT=LLM_Production_Edu
```

파이썬 코드에서 안전하게 로드:

```python
import os
from dotenv import load_dotenv
from openai import OpenAI

# .env 파일에서 환경변수 주입
load_dotenv()

api_key = os.getenv("OPENAI_API_KEY")
if not api_key:
    raise ValueError("OPENAI_API_KEY 환경변수가 설정되지 않았습니다.")

# 클라이언트 인스턴스 생성
client = OpenAI(api_key=api_key)
print("✓ OpenAI 클라이언트 인증 완료")
```

---

## 3. 인터랙티브 월간 API 예산(Budget Limit) 및 초과 방지 시뮬레이터

월간 충전 금액($5 ~ $100)과 알림 임계값(Threshold %)을 설정하면 안전 과금 차단선과 예상 호출 가능 토큰 수가 실시간으로 계산된다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 슬라이더 컨트롤 -->
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:14px">
    <div>
      <div style="display:flex;justify-content:space-between;font-size:12px;font-weight:600;margin-bottom:4px">
        <span>월간 예산 한도 (Monthly Budget):</span>
        <span id="bg-usd-val" style="color:var(--primary);font-weight:700">$ 20.00</span>
      </div>
      <input id="bg-usd-slider" type="range" min="5" max="100" step="5" value="20" style="width:100%;accent-color:var(--primary)" />
    </div>

    <div>
      <div style="display:flex;justify-content:space-between;font-size:12px;font-weight:600;margin-bottom:4px">
        <span>이메일 경고 알림선 (Alert Threshold):</span>
        <span id="bg-alt-val" style="color:var(--chart-2);font-weight:700">80 % ($ 16.00)</span>
      </div>
      <input id="bg-alt-slider" type="range" min="50" max="95" step="5" value="80" style="width:100%;accent-color:var(--primary)" />
    </div>
  </div>

  <!-- 결과 상태 카드 -->
  <div style="display:grid;grid-template-columns:repeat(3, 1fr);gap:8px;background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px;text-align:center">
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">GPT-4o-mini 호출 가능량</div>
      <div id="bg-mini-tok" style="font-size:14px;font-weight:800;color:var(--chart-2);margin-top:2px">53.3 M 토큰</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">GPT-4o 호출 가능량</div>
      <div id="bg-4o-tok" style="font-size:14px;font-weight:800;color:var(--primary);margin-top:2px">3.2 M 토큰</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">비용 통제 상태</div>
      <div style="font-size:13px;font-weight:800;color:var(--chart-2);margin-top:2px">⭐ Hard Limit 보호 활성</div>
    </div>
  </div>
</div>

<script>
(function() {
  var sUsd = document.getElementById('bg-usd-slider');
  var sAlt = document.getElementById('bg-alt-slider');
  var usdVal = document.getElementById('bg-usd-val');
  var altVal = document.getElementById('bg-alt-val');
  var miniTok = document.getElementById('bg-mini-tok');
  var tok4o = document.getElementById('bg-4o-tok');

  function render() {
    var usd = parseInt(sUsd.value, 10);
    var alt = parseInt(sAlt.value, 10);

    usdVal.textContent = '$ ' + usd.toFixed(2);
    altVal.textContent = alt + ' % ($ ' + (usd * alt / 100).toFixed(2) + ')';

    // GPT-4o-mini avg $0.375 / 1M => M tokens = usd / 0.375
    var mMini = usd / 0.375;
    // GPT-4o avg $6.25 / 1M => M tokens = usd / 6.25
    var m4o = usd / 6.25;

    miniTok.textContent = mMini.toFixed(1) + ' M 토큰';
    tok4o.textContent = m4o.toFixed(1) + ' M 토큰';
  }

  sUsd.addEventListener('input', render);
  sAlt.addEventListener('input', render);
  render();
})();
</script>
```
