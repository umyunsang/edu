---
title: "09. LLM 파인튜닝 데이터와 학습 관리 - JSONL 데이터셋 검증과 작업 수명 주기 (Fine-Tuning API)"
description: "OpenAI Fine-Tuning API의 JSONL 훈련 데이터 포맷 검증, 파일 업로드(files.create), 파인튜닝 잡 생성(fine_tuning.jobs.create), 그리고 모델 가중치 미세조정 수명 주기를 인터랙티브 손실 수렴 시뮬레이터로 심층 학습한다."
type: lecture
tags:
  - lecture
  - large-language-models
  - fine-tuning
  - jsonl
  - gpt-4o-mini-fine-tuning
  - transfer-learning
course: large-language-models
semester: extracurricular
source: "OpenAI API.pdf"
source_pages: 15
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

**파인튜닝(Fine-Tuning)**은 사전학습된 거대 언어 모델에 도메인 특화 대화 데이터셋을 전이학습(Transfer Learning)시켜, 프롬프트에 매번 긴 예시(Few-shot)를 주입하지 않고도 높은 정확도와 일관된 출력 형식을 얻는 기법이다.

---

## 1. JSONL 훈련 데이터 포맷 및 유효성 검증

학습 데이터는 한 줄에 하나의 JSON 객체가 포함된 `.jsonl` 형식이어야 한다:

```json
{"messages": [{"role": "system", "content": "당신은 법률 상담 AI입니다."}, {"role": "user", "content": "전세 보증금 반환 절차는 어떻게 되나요?"}, {"role": "assistant", "content": "내용증명 발송 후 임차권등기명령 신청 및 반환 청구 소송을 진행할 수 있습니다."}]}
```

```python
import json

# JSONL 포맷 유효성 검증 스크립트
def validate_jsonl(file_path):
    with open(file_path, "r", encoding="utf-8") as f:
        for idx, line in enumerate(f):
            try:
                data = json.loads(line.strip())
                assert "messages" in data, "messages 키가 누락되었습니다."
                assert len(data["messages"]) >= 2, "최소 2개 이상의 대화가 필요합니다."
            except Exception as e:
                print(f"❌ {idx+1}번째 라인 오류: {e}")
                return False
    print("✓ JSONL 데이터셋 포맷 검증 통과!")
    return True
```

---

## 2. 파인튜닝 수명 주기(Lifecycle) 파이썬 실습 코드

```python
import os
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

# 1. 훈련 파일 업로드 (purpose='fine-tune')
file_obj = client.files.create(
    file=open("train_data.jsonl", "rb"),
    purpose="fine-tune"
)
print(f"업로드된 파일 ID: {file_obj.id}")

# 2. 파인튜닝 작업(Job) 생성
job = client.fine_tuning.jobs.create(
    training_file=file_obj.id,
    model="gpt-4o-mini-2024-07-18",
    hyperparameters={
        "n_epochs": 3,
        "batch_size": "auto",
        "learning_rate_multiplier": "auto"
    }
)
print(f"파인튜닝 작업 시작 ID: {job.id}")

# 3. 작업 진행 상태 확인
status = client.fine_tuning.jobs.retrieve(job.id)
print(f"현재 상태: {status.status}") # validating_files, queued, running, succeeded

# 4. 완료된 파인튜닝 모델로 추론 요청
if status.status == "succeeded":
    fine_tuned_model_id = status.fine_tuned_model
    test_res = client.chat.completions.create(
        model=fine_tuned_model_id,
        messages=[
            {"role": "user", "content": "계약 만료 후 보증금을 못 받고 있습니다."}
        ]
    )
    print("파인튜닝 모델 응답:", test_res.choices[0].message.content)
```

---

## 3. 인터랙티브 파인튜닝 에포크별 손실(Loss) 수렴 시뮬레이터

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 에포크 슬라이더 -->
  <div style="margin-bottom:14px">
    <div style="display:flex;justify-content:space-between;font-size:12px;font-weight:600;margin-bottom:4px">
      <span>학습 에포크 (Epochs):</span>
      <span id="ft-ep-val" style="color:var(--primary);font-weight:700">3 Epochs</span>
    </div>
    <input id="ft-ep-slider" type="range" min="1" max="10" step="1" value="3" style="width:100%;accent-color:var(--primary)" />
  </div>

  <!-- 결과 상태 카드 -->
  <div style="display:grid;grid-template-columns:repeat(3, 1fr);gap:8px;background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px;text-align:center">
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">훈련 손실 (Train Loss)</div>
      <div id="ft-loss-txt" style="font-size:16px;font-weight:800;color:var(--chart-2);margin-top:2px">0.421</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">검증 정확도 (Accuracy)</div>
      <div id="ft-acc-txt" style="font-size:16px;font-weight:800;color:var(--primary);margin-top:2px">94.8%</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">학습 상태 판정</div>
      <div id="ft-fit-txt" style="font-size:13px;font-weight:800;color:var(--chart-2);margin-top:2px">⭐ 최적 수렴 상태</div>
    </div>
  </div>
</div>

<script>
(function() {
  var sEp = document.getElementById('ft-ep-slider');
  var epVal = document.getElementById('ft-ep-val');
  var lossTxt = document.getElementById('ft-loss-txt');
  var accTxt = document.getElementById('ft-acc-txt');
  var fitTxt = document.getElementById('ft-fit-txt');

  function render() {
    var ep = parseInt(sEp.value, 10);
    epVal.textContent = ep + ' Epochs';

    var loss = 2.5 * Math.exp(-0.6 * ep) + 0.15;
    var acc = Math.min(99.2, 50.0 + 48.0 * (1 - Math.exp(-0.7 * ep)));

    lossTxt.textContent = loss.toFixed(3);
    accTxt.textContent = acc.toFixed(1) + '%';

    if (ep >= 2 && ep <= 5) {
      fitTxt.textContent = '⭐ 최적 수렴 상태'; fitTxt.style.color = 'var(--chart-2)';
    } else if (ep < 2) {
      fitTxt.textContent = '⚠️ 과소적합 (Underfitting)'; fitTxt.style.color = 'var(--chart-1)';
    } else {
      fitTxt.textContent = '⚠️ 과적합 위험 (Overfitting)'; fitTxt.style.color = 'var(--destructive)';
    }
  }

  sEp.addEventListener('input', render);
  render();
})();
</script>
```
