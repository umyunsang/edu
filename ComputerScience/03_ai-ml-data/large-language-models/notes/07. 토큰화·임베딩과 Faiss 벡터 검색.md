---
title: "07. 토큰화·임베딩과 Faiss 벡터 검색 - tiktoken과 고성능 유사도 검색 (Tokenization, Embeddings & Faiss)"
description: "tiktoken 라이브러리를 통한 BPE 토큰 분할 및 비용 산정, OpenAI text-embedding-3 모델의 밀집 벡터 변환, 그리고 Faiss 인덱스를 활용한 대규모 벡터 최근접 이웃(KNN) 검색 파이프라인을 인터랙티브 벡터 거리 검색 시뮬레이터로 심층 학습한다."
type: lecture
tags:
  - lecture
  - large-language-models
  - tiktoken
  - text-embedding-3
  - faiss
  - vector-search
  - knn
course: large-language-models
semester: extracurricular
source: "OpenAI API.pdf"
source_pages: 11
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

자연어 텍스트를 LLM에 입력하기 위해서는 **토큰화(Tokenization)**를 거쳐 정수 ID로 변환해야 하며, 시맨틱 검색을 위해서는 고차원 밀집 벡터로 변환하는 **임베딩(Embedding)**과 고속 검색 엔진인 **Faiss**가 필수적이다.

---

## 1. tiktoken 인코딩 모델 및 주요 함수

| 모델 계열 | tiktoken 인코딩 명칭 | 어휘집 크기 ($|V|$) | 주력 사용 모델 |
| :--- | :--- | :--- | :--- |
| `gpt-4o`, `gpt-4o-mini` | `o200k_base` | $200,000$ | 차세대 옴니 멀티모달 모델 (비영어권 토큰 압축률 대폭 향상) |
| `gpt-4`, `gpt-3.5-turbo` | `cl100k_base` | $100,000$ | 표준 챗 완료 모델 |
| `text-davinci-003` | `p50k_base` | $50,000$ | 레거시 GPT-3 완성 모델 |

```python
import tiktoken

# 모델에 맞는 인코딩 로드
enc = tiktoken.encoding_for_model("gpt-4o-mini")

text = "인공지능과 거대언어모델(LLM)을 학습합니다."
token_ids = enc.encode(text)

print(f"토큰 ID 리스트: {token_ids}")
print(f"총 토큰 개수: {len(token_ids)}")
print(f"디코딩 복원 텍스트: {enc.decode(token_ids)}")
```

---

## 2. OpenAI Embedding 생성 및 Faiss 벡터 유사도 검색 실습

```python
import os
import numpy as np
import faiss
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

# 1. 말뭉치(Corpus) 준비
documents = [
    "파이썬은 데이터 과학과 인공지능에 널리 사용되는 프로그래밍 언어입니다.",
    "쿠버네티스는 클라우드 네이티브 환경에서 컨테이너를 관리합니다.",
    "양자 컴퓨팅은 중첩과 얽힘을 이용해 특정 연산을 지수적으로 가속합니다.",
    "트랜스포머의 셀프 어텐션 메커니즘은 자연어 처리의 표준입니다."
]

# 2. 텍스트 임베딩 생성 (text-embedding-3-small: 1536차원)
emb_response = client.embeddings.create(
    model="text-embedding-3-small",
    input=documents
)

doc_vectors = np.array([e.embedding for e in emb_response.data], dtype=np.float32)
# 코사인 유사도 검색을 위한 L2 정규화
faiss.normalize_L2(doc_vectors)

# 3. Faiss 내적(IndexFlatIP) 인덱스 생성 및 벡터 등록
dimension = doc_vectors.shape[1] # 1536
index = faiss.IndexFlatIP(dimension)
index.add(doc_vectors)
print(f"Faiss 인덱스에 등록된 문서 수: {index.ntotal}")

# 4. 사용자 질의(Query) 검색
query = "자연어 처리와 어텐션 모델의 원리"
query_res = client.embeddings.create(model="text-embedding-3-small", input=query)
query_vec = np.array([query_res.data[0].embedding], dtype=np.float32)
faiss.normalize_L2(query_vec)

# 상위 k=2개 검색
k = 2
distances, indices = index.search(query_vec, k)

print(f"\n=== 검색 질의: '{query}' ===")
for rank, (idx, dist) in enumerate(zip(indices[0], distances[0])):
    print(f"[{rank+1}위] (유사도: {dist:.4f}) {documents[idx]}")
```

---

## 3. 인터랙티브 Faiss 최근접 이웃(KNN) 벡터 거리 검색 시뮬레이터

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 질의 선택 바 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <span style="font-size:12px;font-weight:700">검색 쿼리:</span>
    <button id="fss-q-0" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">"어텐션과 LLM 모델"</button>
    <button id="fss-q-1" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">"클라우드 컨테이너 배포"</button>
    <button id="fss-q-2" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">"양자 중첩과 얽힘"</button>
  </div>

  <!-- 검색 순위 결과 리스트 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px">
    <div style="font-size:11px;color:var(--muted-foreground);margin-bottom:8px">Faiss IndexFlatIP Top-3 유사도 랭킹:</div>
    <div id="fss-results" style="display:flex;flex-direction:column;gap:8px"></div>
  </div>
</div>

<script>
(function() {
  var dataset = [
    { text: "트랜스포머의 셀프 어텐션 메커니즘은 자연어 처리의 표준입니다.", sims: [0.924, 0.142, 0.210] },
    { text: "쿠버네티스는 클라우드 네이티브 환경에서 컨테이너를 관리합니다.", sims: [0.180, 0.945, 0.085] },
    { text: "양자 컴퓨팅은 중첩과 얽힘을 이용해 특정 연산을 지수적으로 가속합니다.", sims: [0.220, 0.090, 0.962] },
    { text: "파이썬은 데이터 과학과 인공지능에 널리 사용되는 프로그래밍 언어입니다.", sims: [0.650, 0.320, 0.310] }
  ];

  var curQueryIdx = 0;
  var container = document.getElementById('fss-results');

  function render() {
    // Sort by current query similarity
    var ranked = dataset.slice().sort(function(a, b) {
      return b.sims[curQueryIdx] - a.sims[curQueryIdx];
    });

    container.innerHTML = '';
    ranked.forEach(function(item, rank) {
      var score = item.sims[curQueryIdx];
      var isTop = rank === 0;

      var card = document.createElement('div');
      card.style.display = 'flex';
      card.style.justifyContent = 'space-between';
      card.style.alignItems = 'center';
      card.style.padding = '8px 10px';
      card.style.background = isTop ? 'var(--card)' : 'transparent';
      card.style.border = isTop ? '1px solid var(--primary)' : '1px solid var(--border)';
      card.style.borderRadius = 'var(--radius)';

      var left = document.createElement('div');
      left.style.display = 'flex';
      left.style.alignItems = 'center';
      left.style.gap = '8px';

      var badge = document.createElement('span');
      badge.style.fontSize = '10px';
      badge.style.fontWeight = '800';
      badge.style.padding = '2px 6px';
      badge.style.borderRadius = '3px';
      badge.style.background = isTop ? 'var(--primary)' : 'var(--muted)';
      badge.style.color = isTop ? '#fff' : 'var(--muted-foreground)';
      badge.textContent = '#' + (rank + 1);

      var txt = document.createElement('span');
      txt.style.fontSize = '11px';
      txt.style.color = 'var(--foreground)';
      txt.textContent = item.text;

      left.appendChild(badge);
      left.appendChild(txt);

      var right = document.createElement('div');
      right.style.fontSize = '11px';
      right.style.fontWeight = '800';
      right.style.color = isTop ? 'var(--chart-2)' : 'var(--muted-foreground)';
      right.textContent = (score * 100).toFixed(1) + '%';

      card.appendChild(left);
      card.appendChild(right);
      container.appendChild(card);
    });
  }

  for (var k = 0; k < 3; k++) {
    (function(idx) {
      document.getElementById('fss-q-' + idx).addEventListener('click', function() {
        for (var j = 0; j < 3; j++) {
          var b = document.getElementById('fss-q-' + j);
          if (j === idx) {
            b.style.background = 'var(--primary)'; b.style.color = '#fff';
          } else {
            b.style.background = 'transparent'; b.style.color = 'var(--foreground)';
          }
        }
        curQueryIdx = idx;
        render();
      });
    })(k);
  }

  render();
})();
</script>
```
