---
title: "01. LLM과 자연어 처리 기초 - 통계 언어 모델부터 트랜스포머까지의 발전사 (NLP Foundations to LLM)"
description: "자연어 처리(NLP) 기술의 발전 흐름, 원-핫 인코딩에서 분산 표상(Word2Vec/GloVe), RNN/LSTM 시퀀스 모델의 정보 병목과 기울기 소실, 그리고 셀프 어텐션(Self-Attention) 기반 대규모 언어 모델(LLM)의 패러다임 전환을 인터랙티브 토큰 임베딩 유사도 시뮬레이터로 심층 학습한다."
type: lecture
tags:
  - lecture
  - large-language-models
  - nlp-foundations
  - word-embeddings
  - word2vec
  - rnn
  - lstm
  - self-attention
course: large-language-models
semester: extracurricular
source: "OpenAI API.pdf"
source_pages: 18
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

대규모 언어 모델(**LLM, Large Language Model**)은 수십억 개 이상의 매개변수를 갖추고 인터넷 규모의 방대한 텍스트 코퍼스를 사전학습(Pre-training)하여 인간의 자연어를 이해하고 생성하는 최첨단 딥러닝 모델이다.

---

## 1. 자연어 처리(NLP) 기술의 발전 단계

자연어 처리 패러다임은 이산 기호(Discrete Symbol) 기반의 규칙·통계 모델에서 벡터 공간의 분산 표현(Distributed Representation)을 거쳐, 문맥을 병렬로 파악하는 **어텐션 기반 파운데이션 모델**로 진화하였다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- Step 1 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-1)">1. 규칙 및 통계 기반 (N-gram)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 단어 빈도수(TF-IDF) 및 통계적 동시 발생 확률<br/>
        • 희소성(Sparsity) 문제 및 미등록 단어(OOV) 취약<br/>
        • 어휘 간 시맨틱 관계 표현 불가
      </div>
    </div>
    <!-- Step 2 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">2. 밀집 임베딩 (Word2Vec/GloVe)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • $d$차원 실수 벡터로 단어의 의미적 거리 압축<br/>
        • CBOW / Skip-Gram 분포 가설 기반 학습<br/>
        • 단어의 다의어(Polysemy) 문맥 반영 불가
      </div>
    </div>
    <!-- Step 3 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-3)">3. 순환 신경망 (RNN/LSTM/GRU)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 은닉 상태 $h_t = \tanh(W x_t + U h_{t-1})$ 순차 갱신<br/>
        • 시퀀스 길이 증가 시 기울기 소실/폭주(Vanishing Gradient)<br/>
        • 고정 크기 병목(Bottleneck) 현상
      </div>
    </div>
    <!-- Step 4 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">4. 어텐션 & LLM (Transformer)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 완전 병렬화된 Self-Attention 메커니즘<br/>
        • 장기 의존성(Long-term Dependency) 극복<br/>
        • 수조 개 토큰 기반 다음 토큰 예측(NTP) 사전학습
      </div>
    </div>
  </div>
</div>
```

---

## 2. 자연어 처리 파이프라인 (NLP Pipeline)

자연어 처리 작업은 텍스트 전처리, 토큰화(Tokenization), 수치 벡터화(Vectorization), 모델 추론 및 태스크 수행의 일관된 파이프라인을 거친다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);display:flex;justify-content:center">
  <svg width="480" height="90" style="overflow:visible">
    <!-- 1. Text -->
    <rect x="10" y="25" width="80" height="40" rx="4" fill="var(--background)" stroke="var(--border)" stroke-width="1.5"/>
    <text x="50" y="44" font-size="10" font-weight="700" text-anchor="middle" fill="var(--foreground)">원시 텍스트</text>
    <text x="50" y="56" font-size="8" text-anchor="middle" fill="var(--muted-foreground)">Raw Strings</text>

    <!-- Arrow 1 -->
    <line x1="90" y1="45" x2="120" y2="45" stroke="var(--primary)" stroke-width="2"/>
    <polygon points="120,45 114,41 114,49" fill="var(--primary)"/>

    <!-- 2. Tokenizer -->
    <rect x="120" y="25" width="90" height="40" rx="4" fill="var(--card)" stroke="var(--primary)" stroke-width="1.5"/>
    <text x="165" y="44" font-size="10" font-weight="700" text-anchor="middle" fill="var(--primary)">토큰화 분할</text>
    <text x="165" y="56" font-size="8" text-anchor="middle" fill="var(--muted-foreground)">BPE / WordPiece</text>

    <!-- Arrow 2 -->
    <line x1="210" y1="45" x2="240" y2="45" stroke="var(--primary)" stroke-width="2"/>
    <polygon points="240,45 234,41 234,49" fill="var(--primary)"/>

    <!-- 3. Embedding -->
    <rect x="240" y="25" width="100" height="40" rx="4" fill="var(--background)" stroke="var(--chart-2)" stroke-width="1.5"/>
    <text x="290" y="44" font-size="10" font-weight="700" text-anchor="middle" fill="var(--chart-2)">임베딩 사상</text>
    <text x="290" y="56" font-size="8" text-anchor="middle" fill="var(--muted-foreground)">Vector R^d</text>

    <!-- Arrow 3 -->
    <line x1="340" y1="45" x2="370" y2="45" stroke="var(--primary)" stroke-width="2"/>
    <polygon points="370,45 364,41 364,49" fill="var(--primary)"/>

    <!-- 4. LLM Layers -->
    <rect x="370" y="25" width="100" height="40" rx="4" fill="var(--primary)"/>
    <text x="420" y="44" font-size="10" font-weight="800" text-anchor="middle" fill="#fff">Transformer</text>
    <text x="420" y="56" font-size="8" text-anchor="middle" fill="#eee">NTP / 추론 생성</text>
  </svg>
</div>
```

---

## 3. 원-핫 인코딩 vs 분산 표상(Distributed Representation) 수식 비교

### 원-핫 인코딩 (One-Hot Vector)

어휘집 크기 $|V|$에서 특정 단어 $w_i$만 1이고 나머지는 0인 희소 벡터:

$$\mathbf{v}(w_i) = [0, \dots, 0, \underbrace{1}_{i\text{-th}}, 0, \dots, 0]^T \in \{0, 1\}^{|V|}$$

임의의 서로 다른 두 단어 $w_i \ne w_j$의 내적(유사도)은 항상 0이다:

$$\mathbf{v}(w_i) \cdot \mathbf{v}(w_j) = 0 \quad (\text{단어 간 의미적 연관성 측정 불가})$$

### 밀집 임베딩 (Dense Embedding)

단어를 $d$차원 ($d \ll |V|$) 연속 벡터 공간 $\mathbb{R}^d$에 사상하여 코사인 유사도($S_C$) 계산 가능:

$$S_C(\mathbf{u}, \mathbf{v}) = \frac{\mathbf{u} \cdot \mathbf{v}}{\|\mathbf{u}\|_2 \|\mathbf{v}\|_2} = \frac{\sum_{k=1}^d u_k v_k}{\sqrt{\sum_{k=1}^d u_k^2} \sqrt{\sum_{k=1}^d v_k^2}}$$

---

## 4. 인터랙티브 단어 임베딩 코사인 유사도(Cosine Similarity) 시뮬레이터

두 단어 벡터의 내적과 각도($\theta$)를 조절하면 코사인 유사도 $S_C \in [-1, 1]$ 및 의미적 거리 판정이 실시간으로 계산된다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 단어 쌍 선택 버튼 바 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <span style="font-size:12px;font-weight:700">단어 쌍 프리셋:</span>
    <button id="emb-pair-0" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">왕(King) & 여왕(Queen)</button>
    <button id="emb-pair-1" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">인공지능(AI) & 딥러닝(DL)</button>
    <button id="emb-pair-2" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">사과(Apple) & 우주선(Rocket)</button>
  </div>

  <!-- 각도 슬라이더 -->
  <div style="margin-bottom:14px">
    <div style="display:flex;justify-content:space-between;font-size:12px;font-weight:600;margin-bottom:4px">
      <span>벡터 사이의 사잇각 (θ, Angle):</span>
      <span id="emb-ang-val" style="color:var(--primary);font-weight:700">θ = 25.8°</span>
    </div>
    <input id="emb-ang-slider" type="range" min="0" max="180" step="1" value="26" style="width:100%;accent-color:var(--primary)" />
  </div>

  <!-- 결과 상태 카드 -->
  <div style="display:grid;grid-template-columns:repeat(3, 1fr);gap:8px;background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px;text-align:center">
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">코사인 유사도 (cos θ)</div>
      <div id="emb-cos-txt" style="font-size:16px;font-weight:800;color:var(--chart-2);margin-top:2px">0.899</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">유클리드 거리 (L2)</div>
      <div id="emb-l2-txt" style="font-size:16px;font-weight:800;color:var(--primary);margin-top:2px">0.450</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">시맨틱 유사성 판정</div>
      <div id="emb-eval-txt" style="font-size:13px;font-weight:800;color:var(--chart-2);margin-top:2px">⭐ 매우 밀접한 연관 단어</div>
    </div>
  </div>
</div>

<script>
(function() {
  var presets = [
    { name: '왕(King) & 여왕(Queen)', deg: 26 },
    { name: '인공지능(AI) & 딥러닝(DL)', deg: 18 },
    { name: '사과(Apple) & 우주선(Rocket)', deg: 88 }
  ];

  var sAng = document.getElementById('emb-ang-slider');
  var angVal = document.getElementById('emb-ang-val');
  var cosTxt = document.getElementById('emb-cos-txt');
  var l2Txt = document.getElementById('emb-l2-txt');
  var evalTxt = document.getElementById('emb-eval-txt');

  function render() {
    var deg = parseInt(sAng.value, 10);
    angVal.textContent = 'θ = ' + deg + '°';

    var rad = (deg * Math.PI) / 180.0;
    var sim = Math.cos(rad);
    // 단위 벡터 가정 시 L2 = sqrt(2 - 2*cos(rad))
    var l2 = Math.sqrt(Math.max(0, 2 - 2 * sim));

    cosTxt.textContent = sim.toFixed(3);
    l2Txt.textContent = l2.toFixed(3);

    if (sim > 0.8) {
      evalTxt.textContent = '⭐ 매우 밀접한 연관 단어'; evalTxt.style.color = 'var(--chart-2)';
      cosTxt.style.color = 'var(--chart-2)';
    } else if (sim > 0.3) {
      evalTxt.textContent = '일반적 문맥 공유 단어'; evalTxt.style.color = 'var(--primary)';
      cosTxt.style.color = 'var(--primary)';
    } else if (sim >= 0) {
      evalTxt.textContent = '무관한 직교 단어'; evalTxt.style.color = 'var(--muted-foreground)';
      cosTxt.style.color = 'var(--muted-foreground)';
    } else {
      evalTxt.textContent = '반대 개념의 단어'; evalTxt.style.color = 'var(--destructive)';
      cosTxt.style.color = 'var(--destructive)';
    }
  }

  for (var k = 0; k < 3; k++) {
    (function(idx) {
      document.getElementById('emb-pair-' + idx).addEventListener('click', function() {
        for (var j = 0; j < 3; j++) {
          var b = document.getElementById('emb-pair-' + j);
          if (j === idx) {
            b.style.background = 'var(--primary)'; b.style.color = '#fff';
          } else {
            b.style.background = 'transparent'; b.style.color = 'var(--foreground)';
          }
        }
        sAng.value = presets[idx].deg;
        render();
      });
    })(k);
  }

  sAng.addEventListener('input', render);
  render();
})();
</script>
```
