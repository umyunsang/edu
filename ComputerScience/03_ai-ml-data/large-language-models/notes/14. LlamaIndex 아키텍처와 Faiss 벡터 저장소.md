---
title: "14. LlamaIndex 아키텍처와 Faiss 벡터 저장소 - 데이터 인덱싱과 쿼리 엔진 (LlamaIndex & Faiss)"
description: "LlamaIndex의 핵심 구성 요소(Data Connectors, VectorStoreIndex, Query Engine), FaissVectorStore를 통한 고속 인덱싱 연동, 그리고 Ollama 로컬 LLM 통합을 인터랙티브 인덱스 쿼리 라우팅 시뮬레이터로 심층 학습한다."
type: lecture
tags:
  - lecture
  - large-language-models
  - llamaindex
  - simpledirectoryreader
  - vectorstoreindex
  - faiss-vectorstore
  - query-engine
course: large-language-models
semester: extracurricular
source: "RAG_LangChain.pdf"
source_pages: 19
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

**LlamaIndex**는 비정형 사내 데이터를 손쉽게 수집(Loading), 구조화된 지식 인덱스로 변환(Indexing), 그리고 자연어 질의응답 인터페이스(Querying)로 제공하는 데이터 중심의 RAG 전용 프레임워크이다.

---

## 1. LlamaIndex 핵심 3대 구성 계층

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- 1. Loading -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-1)">1. 로딩 (Loading)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • <code>SimpleDirectoryReader</code>를 통해 PDF, Word, PPT, 코드 파일 일괄 수집<br/>
        • 100개 이상의 LlamaHub 커넥터 지원
      </div>
    </div>
    <!-- 2. Indexing -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">2. 색인 (Indexing)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • <code>VectorStoreIndex</code>: 임베딩 기반 계층 노드 트리 구성<br/>
        • Faiss, Chroma, Pinecone 등 벡터 저장소 플러그인
      </div>
    </div>
    <!-- 3. Querying -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">3. 질의 (Query Engine)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • <code>index.as_query_engine()</code>: 합성 및 응답<br/>
        • <code>index.as_chat_engine()</code>: 이전 대화 기억 유지
      </div>
    </div>
  </div>
</div>
```

---

## 2. LlamaIndex + Faiss 벡터 저장소 파이썬 실습 코드

```python
import os
import faiss
from llama_index.core import VectorStoreIndex, SimpleDirectoryReader, StorageContext
from llama_index.vector_stores.faiss import FaissVectorStore
from dotenv import load_dotenv

load_dotenv()

# 1. 문서 로딩
documents = SimpleDirectoryReader("data").load_data()
print(f"로드된 문서 수: {len(documents)} 개")

# 2. Faiss 1536차원 인덱스 생성 및 LlamaIndex 벡터 스토어 바인딩
d = 1536
faiss_index = faiss.IndexFlatIP(d)
vector_store = FaissVectorStore(faiss_index=faiss_index)
storage_context = StorageContext.from_defaults(vector_store=vector_store)

# 3. 인덱스 빌드 및 질의 엔진 생성
index = VectorStoreIndex.from_documents(
    documents,
    storage_context=storage_context
)

query_engine = index.as_query_engine(similarity_top_k=3)
response = query_engine.query("프로젝트의 핵심 목표와 배포 일정을 요약해줘.")

print("\n=== LlamaIndex 응답 ===")
print(response)
```

---

## 3. 인터랙티브 LlamaIndex 검색 노드 합성 시뮬레이터

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 상단 컨트롤 -->
  <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:12px">
    <span style="font-size:12px;font-weight:700">LlamaIndex 합성 모드:</span>
    <select id="llam-mode-sel" style="padding:6px;background:var(--background);border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px">
      <option value="compact">compact (컨텍스트 압축 결합 - 기본값)</option>
      <option value="refine">refine (순차 노드 반복 정제)</option>
      <option value="tree_summarize">tree_summarize (계층형 트리 요약)</option>
    </select>
  </div>

  <!-- 결과 상태 카드 -->
  <div style="display:grid;grid-template-columns:repeat(3, 1fr);gap:8px;background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px;text-align:center">
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">LLM API 호출 횟수</div>
      <div id="llam-calls-txt" style="font-size:15px;font-weight:800;color:var(--chart-2);margin-top:2px">1 회</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">응답 지연 시간</div>
      <div id="llam-lat-txt" style="font-size:15px;font-weight:800;color:var(--primary);margin-top:2px">빠름 (0.6s)</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">적합 사용 사례</div>
      <div id="llam-use-txt" style="font-size:12px;font-weight:800;color:var(--chart-2);margin-top:2px">일반 Q&A</div>
    </div>
  </div>
</div>

<script>
(function() {
  var sMode = document.getElementById('llam-mode-sel');
  var callsTxt = document.getElementById('llam-calls-txt');
  var latTxt = document.getElementById('llam-lat-txt');
  var useTxt = document.getElementById('llam-use-txt');

  function render() {
    var mode = sMode.value;
    if (mode === 'compact') {
      callsTxt.textContent = '1 회'; callsTxt.style.color = 'var(--chart-2)';
      latTxt.textContent = '빠름 (0.6s)';
      useTxt.textContent = '일반 Q&A';
    } else if (mode === 'refine') {
      callsTxt.textContent = 'N 회 (노드 수)'; callsTxt.style.color = 'var(--chart-1)';
      latTxt.textContent = '보통 (2.4s)';
      useTxt.textContent = '정밀 분석';
    } else {
      callsTxt.textContent = 'log2(N) 회'; callsTxt.style.color = 'var(--primary)';
      latTxt.textContent = '보통 (1.8s)';
      useTxt.textContent = '대량 문서 요약';
    }
  }

  sMode.addEventListener('change', render);
  render();
})();
</script>
```
