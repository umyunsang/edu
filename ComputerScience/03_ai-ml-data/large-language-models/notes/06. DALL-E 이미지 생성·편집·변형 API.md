---
title: "06. DALL-E 이미지 생성·편집·변형 API - 텍스트-투-이미지와 마스킹 인페인팅 (DALL-E Image API)"
description: "OpenAI Images API를 활용한 DALL-E 3 텍스트 프롬프트 이미지 생성, DALL-E 2 마스크 기반 영역 편집(Inpainting), 그리고 이미지 변형(Variation) 생성 파이프라인을 인터랙티브 이미지 생성 비용 계산기로 심층 학습한다."
type: lecture
tags:
  - lecture
  - large-language-models
  - image-generation
  - dall-e-3
  - inpainting
  - variation
course: large-language-models
semester: extracurricular
source: "OpenAI API.pdf"
source_pages: 7
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

OpenAI의 **Images API**는 자연어 텍스트 프롬프트를 바탕으로 고해상도 이미지를 생성(`generate`), 기존 이미지의 특정 영역을 마스킹하여 수정(`edit`), 또는 원본의 분위기를 유지하며 새로운 구도를 합성(`create_variation`)하는 기능을 제공한다.

---

## 1. Images API의 3대 핵심 기능

| 기능 | API 메서드 | 주요 매개변수 | 설명 |
| :--- | :--- | :--- | :--- |
| **텍스트 생성** | `client.images.generate()` | `prompt`, `model`, `size`, `quality`, `style` | 텍스트 지시문으로부터 새로운 이미지 생성 (DALL-E 3 지원) |
| **마스크 편집** | `client.images.edit()` | `image`, `mask`, `prompt`, `size` | 원본 이미지에서 마스크(투명 RGBA) 영역만 프롬프트로 재합성 |
| **이미지 변형** | `client.images.create_variation()` | `image`, `n`, `size` | 원본 이미지의 특징을 보존하며 새로운 구도/스타일 변형 생성 |

---

## 2. 파이썬 이미지 생성 및 마스킹 편집 실습 코드

```python
import os
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

# 1. DALL-E 3 텍스트 프롬프트 이미지 생성
gen_response = client.images.generate(
    model="dall-e-3",
    prompt="미래지향적인 스마트 시티의 야경을 그린 사이버펑크 스타일 디지털 아트",
    size="1024x1024",
    quality="standard", # standard 또는 hd
    style="vivid",      # vivid (극적/초현실적) 또는 natural (자연스러운 톤)
    n=1
)
image_url = gen_response.data[0].url
print(f"생성된 이미지 URL: {image_url}")

# 2. 마스크 기반 이미지 편집 (Inpainting) 실습
# 주의: image와 mask는 RGBA 4채널 PNG 파일이어야 함
with open("original.png", "rb") as img_file, open("mask.png", "rb") as mask_file:
    edit_response = client.images.edit(
        model="dall-e-2",
        image=img_file,
        mask=mask_file,
        prompt="맑은 하늘에 떠 있는 붉은색 열기구",
        n=1,
        size="1024x1024"
    )
print(f"편집된 이미지 URL: {edit_response.data[0].url}")
```

---

## 3. 인터랙티브 DALL-E 3 해상도·품질별 비용 계산기

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 옵션 선택 그리드 -->
  <div style="display:grid;grid-template-columns:1fr 1fr 1fr;gap:10px;margin-bottom:14px">
    <div>
      <label style="font-size:11px;font-weight:700;display:block;margin-bottom:4px">해상도 (Resolution):</label>
      <select id="img-res-sel" style="width:100%;padding:6px;background:var(--background);border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px">
        <option value="1024">1024 x 1024 (정사각형)</option>
        <option value="1792">1024 x 1792 (와이드/포트레이트)</option>
      </select>
    </div>

    <div>
      <label style="font-size:11px;font-weight:700;display:block;margin-bottom:4px">품질 (Quality):</label>
      <select id="img-qty-sel" style="width:100%;padding:6px;background:var(--background);border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px">
        <option value="std">Standard ($0.040 ~ $0.080)</option>
        <option value="hd">HD ($0.080 ~ $0.120)</option>
      </select>
    </div>

    <div>
      <label style="font-size:11px;font-weight:700;display:block;margin-bottom:4px">생성 수량 (장):</label>
      <input id="img-cnt-inp" type="number" min="1" max="100" value="10" style="width:100%;padding:5px;background:var(--background);border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px" />
    </div>
  </div>

  <!-- 결과 상태 카드 -->
  <div style="display:grid;grid-template-columns:repeat(3, 1fr);gap:8px;background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px;text-align:center">
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">장당 단가</div>
      <div id="img-unit-cost" style="font-size:15px;font-weight:800;color:var(--chart-2);margin-top:2px">$ 0.040</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">총 예상 비용</div>
      <div id="img-total-cost" style="font-size:15px;font-weight:800;color:var(--primary);margin-top:2px">$ 0.400</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">권장 스타일 모드</div>
      <div style="font-size:12px;font-weight:800;color:var(--chart-2);margin-top:2px">vivid (생동감 극대화)</div>
    </div>
  </div>
</div>

<script>
(function() {
  var sRes = document.getElementById('img-res-sel');
  var sQty = document.getElementById('img-qty-sel');
  var sCnt = document.getElementById('img-cnt-inp');
  var uCost = document.getElementById('img-unit-cost');
  var tCost = document.getElementById('img-total-cost');

  function render() {
    var res = sRes.value;
    var qty = sQty.value;
    var cnt = Math.max(1, parseInt(sCnt.value, 10) || 1);

    var unit = 0.040;
    if (res === '1024' && qty === 'std') unit = 0.040;
    else if (res === '1024' && qty === 'hd') unit = 0.080;
    else if (res === '1792' && qty === 'std') unit = 0.080;
    else if (res === '1792' && qty === 'hd') unit = 0.120;

    var total = unit * cnt;
    uCost.textContent = '$ ' + unit.toFixed(3);
    tCost.textContent = '$ ' + total.toFixed(3);
  }

  sRes.addEventListener('change', render);
  sQty.addEventListener('change', render);
  sCnt.addEventListener('input', render);
  render();
})();
</script>
```
