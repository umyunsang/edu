---
title: "18. Few-shot 프롬프트와 예제 선택기 - 고정 예제와 동적 유사도 선택 (Few-Shot & Example Selectors)"
description: "FewShotPromptTemplate을 통한 예시 기반 인컨텍스트 학습(In-Context Learning), 그리고 길이 기반(LengthBased) 및 임베딩 유사도 기반(SemanticSimilarity) ExampleSelector를 인터랙티브 예제 선택 시뮬레이터로 심층 학습한다."
type: lecture
tags:
  - lecture
  - large-language-models
  - few-shot
  - example-selector
  - semantic-similarity
  - length-based-selector
  - in-context-learning
course: large-language-models
semester: extracurricular
source: "RAG_LangChain.pdf"
source_pages: 15
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

**Few-shot 프롬프팅**은 모델에게 몇 가지 입력-출력 예시 쌍을 직접 제공하여 지시문만으로는 모호할 수 있는 출력 스타일, 특수 문법, 분류 기준을 즉각 적응시키는 강력한 인컨텍스트 학습 기법이다.

---

## 1. ExampleSelector의 2대 동적 선별 전략

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
    <!-- LengthBased -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary);margin-bottom:4px">1. LengthBasedExampleSelector</div>
      <div style="font-size:10px;color:var(--muted-foreground);line-height:1.5">
        • 사용자 질문 길이에 따라 예제 개수를 자동 증감<br/>
        • 컨텍스트 토큰 상한(`max_length`)을 엄격히 준수하여 에러 방지
      </div>
    </div>
    <!-- SemanticSimilarity -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2);margin-bottom:4px">2. SemanticSimilarityExampleSelector</div>
      <div style="font-size:10px;color:var(--muted-foreground);line-height:1.5">
        • 수백 개의 예제 풀에서 입력과 벡터 유사도가 가장 높은 Top-k 추출<br/>
        • 질문 도메인에 가장 적합한 고품질 레퍼런스만 선별 주입
      </div>
    </div>
  </div>
</div>
```

---

## 2. 동적 Few-shot 프롬프트 파이썬 실습 코드

```python
from langchain_core.prompts import PromptTemplate, FewShotPromptTemplate
from langchain_core.example_selectors import LengthBasedExampleSelector

# 1. 예제 데이터셋 정의 (반의어 생성 태스크)
examples = [
    {"input": "행복", "output": "슬픔"},
    {"input": "높다", "output": "낮다"},
    {"input": "빠르다", "output": "느리다"},
    {"input": "밝다", "output": "어둡다"},
    {"input": "거대하다", "output": "왜소하다"}
]

example_prompt = PromptTemplate(
    input_variables=["input", "output"],
    template="단어: {input}\n반의어: {output}"
)

# 2. 길이 기반 예제 선택기 생성 (최대 100자 이내로 예제 제한)
example_selector = LengthBasedExampleSelector(
    examples=examples,
    example_prompt=example_prompt,
    max_length=100
)

# 3. FewShotPromptTemplate 구성
dynamic_prompt = FewShotPromptTemplate(
    example_selector=example_selector,
    example_prompt=example_prompt,
    prefix="주어진 단어의 반의어를 작성하세요.",
    suffix="단어: {input}\n반의어:",
    input_variables=["input"]
)

# 짧은 입력 시 예제들이 풍부하게 포함됨
formatted = dynamic_prompt.format(input="뜨겁다")
print("=== 완성된 동적 Few-shot 프롬프트 ===")
print(formatted)
```

---

## 3. 인터랙티브 Dynamic Few-shot 예제 선별 시뮬레이터

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 입력 선택 바 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <span style="font-size:12px;font-weight:700">입력 문장 길이:</span>
    <button id="few-btn-short" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">짧은 입력 ("행복")</button>
    <button id="few-btn-long" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">긴 복합 입력 (120자 이상)</button>
  </div>

  <!-- 결과 상태 카드 -->
  <div style="display:grid;grid-template-columns:repeat(3, 1fr);gap:8px;background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px;text-align:center">
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">주입된 예제 개수</div>
      <div id="few-ex-cnt" style="font-size:16px;font-weight:800;color:var(--chart-2);margin-top:2px">4 개 (전체 주입)</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">프롬프트 총 글자 수</div>
      <div id="few-len-txt" style="font-size:16px;font-weight:800;color:var(--primary);margin-top:2px">84 자</div>
    </div>
    <div style="padding:8px;background:var(--card);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--muted-foreground)">컨텍스트 안전도</div>
      <div style="font-size:12px;font-weight:800;color:var(--chart-2);margin-top:2px">⭐ 완벽 (한도 내)</div>
    </div>
  </div>
</div>

<script>
(function() {
  var bShort = document.getElementById('few-btn-short');
  var bLong = document.getElementById('few-btn-long');
  var exCnt = document.getElementById('few-ex-cnt');
  var lenTxt = document.getElementById('few-len-txt');

  bShort.addEventListener('click', function() {
    bShort.style.background = 'var(--primary)'; bShort.style.color = '#fff';
    bLong.style.background = 'transparent'; bLong.style.color = 'var(--foreground)';
    exCnt.textContent = '4 개 (전체 주입)';
    lenTxt.textContent = '84 자';
  });

  bLong.addEventListener('click', function() {
    bLong.style.background = 'var(--primary)'; bLong.style.color = '#fff';
    bShort.style.background = 'transparent'; bShort.style.color = 'var(--foreground)';
    exCnt.textContent = '1 개 (동적 축소)';
    lenTxt.textContent = '142 자';
  });
})();
</script>
```
