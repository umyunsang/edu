---
title: "00. generative-ai-fine-tuning 강의 흐름 지도 - 생성형 AI 파인튜닝 전 단원 로드맵 (Curriculum Map)"
description: "생성형 AI와 파인튜닝 교과목의 4대 핵심 영역(생성 AI 파이프라인과 LDM 멀티모달, ControlNet 공간 제어와 이미지 LoRA, 대화형 SFT와 ChatML 손실 마스킹, 제조 생성 설계 및 RLHF/DPO 정렬)을 인터랙티브 커리큘럼 맵으로 조망한다."
type: lecture
tags:
  - lecture
  - generative-ai-fine-tuning
  - curriculum-map
  - road-map
course: generative-ai-fine-tuning
semester: extracurricular
source: "sources/*.pdf"
source_pages: 350
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

본 강의 흐름 지도는 **생성형 AI와 파인튜닝(Generative AI & Fine-Tuning)** 교과목의 전 단원 강의 노트를 4대 핵심 학습 스테이지로 구조화하여 제공한다.

---

## 1. 4대 핵심 생성형 AI 학습 스테이지

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- Stage 1 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-1)">Stage 1: LDM & Multimodal</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 파운데이션 모델 & 3단계 학습 파이프라인<br/>
        • 잠재 확산 모델(LDM) & VAE/CLIP<br/>
        • Cross-Attention 조건화 메커니즘
      </div>
    </div>
    <!-- Stage 2 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">Stage 2: Control & LoRA</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • ControlNet Zero Conv 공간 제어<br/>
        • LoRA 저순위 분해 & QLoRA 4-bit NF4<br/>
        • 이미지 LoRA 훈련 및 정규화 데이터
      </div>
    </div>
    <!-- Stage 3 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-3)">Stage 3: ChatML & SFT</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 대화형 ChatML 구분자 템플릿<br/>
        • 프롬프트 손실 마스킹 (`label = -100`)<br/>
        • 고품질 인스트럭션 데이터 큐레이션
      </div>
    </div>
    <!-- Stage 4 -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">Stage 4: Manufacturing & DPO</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • 제조 위상 최적화(SIMP) & FEA 해석<br/>
        • Von Mises 응력 및 안전 계수(SF)<br/>
        • PPO vs DPO 직접 선호 최적화 정렬
      </div>
    </div>
  </div>
</div>
```

---

## 2. 인터랙티브 생성 AI 커리큘럼 네비게이터

스테이지 버튼을 클릭하면 해당 단원의 세부 학습 주제와 핵심 요약이 실시간으로 전환된다.

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 스테이지 선택 탭 -->
  <div style="display:flex;gap:6px;align-items:center;margin-bottom:14px;flex-wrap:wrap">
    <button id="gen-btn-1" style="padding:6px 10px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">1. 파이프라인 & LDM (01, 15)</button>
    <button id="gen-btn-2" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">2. 제어 & LoRA (26, 40, 50)</button>
    <button id="gen-btn-3" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">3. SFT & ChatML (46)</button>
    <button id="gen-btn-4" style="padding:6px 10px;background:transparent;border:1px solid var(--border);color:var(--foreground);border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">4. 제조설계 & DPO (60, 72)</button>
  </div>

  <!-- 세부 컨테이너 -->
  <div style="background:var(--background);border:2px dashed var(--border);border-radius:var(--radius);padding:14px">
    <div id="gen-title" style="font-size:14px;font-weight:800;color:var(--primary);margin-bottom:8px">
      Stage 1: 파운데이션 모델과 LDM 멀티모달 생성
    </div>
    <div id="gen-desc" style="font-size:12px;color:var(--muted-foreground);line-height:1.6;margin-bottom:10px">
      생성 AI의 3단계 학습 파이프라인, 풀 파인튜닝과 PEFT 연산 비용, 잠재 확산 모델(LDM)의 VAE/CLIP/UNet 구조.
    </div>
    <div id="gen-list" style="display:flex;flex-direction:column;gap:6px"></div>
  </div>
</div>

<script>
(function() {
  var stages = [
    {
      title: 'Stage 1: 파운데이션 모델과 LDM 멀티모달 생성 (Notes 01, 15)',
      desc: '생성 AI의 3단계 학습 파이프라인, 풀 파인튜닝과 PEFT 연산 비용, 잠재 확산 모델(LDM)의 VAE/CLIP/UNet 구조.',
      notes: [
        '01. 생성형 AI와 파인튜닝 - LLM 진화와 3단계 학습 파이프라인',
        '15. 멀티모달 생성 워크플로 - 잠재 확산 모델과 Cross-Attention 메커니즘'
      ]
    },
    {
      title: 'Stage 2: 공간 제어와 LoRA/QLoRA 경량화 (Notes 26, 40, 50)',
      desc: 'ControlNet Zero Conv 공간 제어, IP-Adapter 일관성 유지, LoRA 저순위 분해, QLoRA NF4 양자화, 이미지 LoRA 훈련.',
      notes: [
        '26. 디자인 탐색과 제어 - ControlNet 공간 제어와 IP-Adapter 일관성 유지',
        '40. LoRA와 파라미터 효율 미세조정 - 저순위 분해와 QLoRA 양자화 기술',
        '50. 이미지 LoRA 학습과 적용 - 확산 모델 어댑터와 정규화 기법'
      ]
    },
    {
      title: 'Stage 3: 지시문 데이터와 ChatML 미세조정 (Note 46)',
      desc: '대화형 ChatML 템플릿, 프롬프트 손실 마스킹(Loss Masking with label -100), SFT 데이터셋 품질 필터링.',
      notes: [
        '46. 지시문 데이터와 대화 파인튜닝 - ChatML 템플릿과 손실 마스킹'
      ]
    },
    {
      title: 'Stage 4: 제조 생성 설계와 RLHF/DPO 가치 정렬 (Notes 60, 72)',
      desc: '제조 위상 최적화(SIMP)와 FEA 응력 해석, PPO 액터-크리틱 RLHF 및 DPO 직접 선호 최적화 수렴.',
      notes: [
        '60. 제조 생성 설계와 성능 평가 - 위상 최적화와 유한요소해석(FEA) 통합',
        '72. 강화학습과 생성 설계의 기초 - RLHF와 Direct Preference Optimization'
      ]
    }
  ];

  var sTitle = document.getElementById('gen-title');
  var sDesc = document.getElementById('gen-desc');
  var sList = document.getElementById('gen-list');

  function setStage(idx) {
    for (var i = 1; i <= 4; i++) {
      var btn = document.getElementById('gen-btn-' + i);
      if (i === idx + 1) {
        btn.style.background = 'var(--primary)';
        btn.style.color = '#fff';
      } else {
        btn.style.background = 'transparent';
        btn.style.color = 'var(--foreground)';
      }
    }

    var st = stages[idx];
    sTitle.textContent = st.title;
    sDesc.textContent = st.desc;
    sList.innerHTML = '';

    st.notes.forEach(function(n) {
      var d = document.createElement('div');
      d.style.padding = '8px 12px';
      d.style.background = 'var(--card)';
      d.style.border = '1px solid var(--border)';
      d.style.borderRadius = 'var(--radius)';
      d.style.fontSize = '12px';
      d.style.fontWeight = '600';
      d.style.color = 'var(--foreground)';
      d.textContent = '📄 ' + n;
      sList.appendChild(d);
    });
  }

  for (var k = 1; k <= 4; k++) {
    (function(idx) {
      document.getElementById('gen-btn-' + (idx + 1)).addEventListener('click', function() {
        setStage(idx);
      });
    })(k - 1);
  }

  setStage(0);
})();
</script>
```
