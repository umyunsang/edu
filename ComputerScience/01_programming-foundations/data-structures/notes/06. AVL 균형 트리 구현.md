---
title: "06. AVL 균형 트리 구현 - 균형 인수(BF) 판별과 4대 회전(LL·RR·LR·RL) 대수학 (AVL Balanced Trees & Rotations)"
description: "AVL 트리의 균형 인수(Balance Factor) 수학적 정의, 엄격한 O(log N) 높이 상한 보장 증명, LL·RR 단순 회전과 LR·RL 이중 회전의 알고리즘을 인터랙티브 AVL 회전 시뮬레이터로 심층 학습한다."
type: lecture
tags:
  - lecture
  - data-structures
  - avl-tree
  - balance-factor
  - tree-rotations
  - ll-rotation
  - rr-rotation
  - lr-rotation
  - rl-rotation
course: data-structures
semester: "2024-1"
source: "11주차(AVL).pdf; 11주차(AVL코드구현).pdf"
source_pages: 45
source_type: "lecture-slides"
status: draft
review_state: active
authority: primary
slides: true
created: "2026-08-29"
updated: "2026-08-29"
---

\*\*AVL 트리(Adelson-Velsky and Landis Tree)\*\*는 모든 노드에서 왼쪽 서브트리와 오른쪽 서브트리의 높이 차이가 최대 1 이하로 유지되도록 스스로 균형을 잡는 \*\*자가 균형 이진 탐색 트리(Self-Balancing BST)\*\*이다.

---

## 1. 균형 인수(Balance Factor)와 높이 상한의 수학적 공식화

### 1) 노드 높이 및 균형 인수(BF)의 엄밀한 정의

임의의 노드 $v$에 대해:

$$h(v) = 1 + \max\big( h(\text{Left}(v)), \; h(\text{Right}(v)) \big)$$

$$BF(v) = h(\text{Left}(v)) - h(\text{Right}(v))$$

AVL 트리의 불변식(Invariant): 모든 노드 $v$에 대해 $BF(v) \in {-1, 0, +1}$.

### 2) AVL 트리의 최대 높이 상한 증명

높이가 $h$인 AVL 트리가 가질 수 있는 최소 노드 수 $N(h)$는 피보나치 수열과 유사한 점화식을 따른다:

$$N(h) = N(h-1) + N(h-2) + 1 \implies h < 1.44 \log_2(n + 2) \implies \text{탐색 복잡도 } \mathcal{O}(\log n)$$

---

## 2. 불균형 유형과 4대 회전(Rotation) 알고리즘

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:16px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius)">
  <div style="display:grid;grid-template-columns:repeat(auto-fit, minmax(130px, 1fr));gap:8px">
    <!-- LL -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--primary);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--primary)">1. LL 회전 (Right)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • $BF \ge +2$, 왼쪽 자식의 $BF > 0$<br/>
        • <strong>조치</strong>: 불균형 노드 기준 1회 우회전(Right Rotate)
      </div>
    </div>
    <!-- RR -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--chart-2);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-2)">2. RR 회전 (Left)</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • $BF \le -2$, 오른쪽 자식의 $BF < 0$<br/>
        • <strong>조치</strong>: 불균형 노드 기준 1회 좌회전(Left Rotate)
      </div>
    </div>
    <!-- LR -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--chart-3);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--chart-3)">3. LR 이중 회전</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • $BF \ge +2$, 왼쪽 자식의 $BF < 0$<br/>
        • <strong>조치</strong>: 왼쪽 자식 좌회전 ➔ 부모 우회전
      </div>
    </div>
    <!-- RL -->
    <div style="padding:10px;background:var(--background);border:1px solid var(--border);border-radius:var(--radius)">
      <div style="font-size:11px;font-weight:800;color:var(--foreground)">4. RL 이중 회전</div>
      <div style="font-size:10px;color:var(--muted-foreground);margin-top:4px">
        • $BF \le -2$, 오른쪽 자식의 $BF > 0$<br/>
        • <strong>조치</strong>: 오른쪽 자식 우회전 ➔ 부모 좌회전
      </div>
    </div>
  </div>
</div>
```

---

## 3. C 언어 AVL 노드 우회전(Right Rotation) 구현 코드

```c
#include <stdio.h>
#include <stdlib.h>

typedef struct AVLNode {
    int key;
    int height;
    struct AVLNode *left;
    struct AVLNode *right;
} AVLNode;

int get_height(AVLNode *n) {
    return (n == NULL) ? 0 : n->height;
}

int max(int a, int b) {
    return (a > b) ? a : b;
}

// LL 불균형 해소를 위한 우회전 (Right Rotation)
AVLNode *rotate_right(AVLNode *y) {
    AVLNode *x = y->left;
    AVLNode *T2 = x->right;

    // 회전 수행
    x->right = y;
    y->left = T2;

    // 높이 갱신
    y->height = max(get_height(y->left), get_height(y->right)) + 1;
    x->height = max(get_height(x->left), get_height(x->right)) + 1;

    return x; // 새로운 루트 반환
}
```

---

## 4. 인터랙티브 AVL 4대 회전 시뮬레이터

```html preview
<div style="font-family:system-ui,-apple-system,sans-serif;padding:20px;background:var(--card);color:var(--card-foreground);border:1px solid var(--border);border-radius:var(--radius);max-width:100%">
  <!-- 회전 모드 선택 바 -->
  <div style="display:flex;gap:8px;margin-bottom:14px;flex-wrap:wrap">
    <button id="avl-btn-ll" style="padding:6px 12px;background:var(--primary);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">LL 불균형 (우회전)</button>
    <button id="avl-btn-rr" style="padding:6px 12px;background:var(--chart-2);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">RR 불균형 (좌회전)</button>
    <button id="avl-btn-lr" style="padding:6px 12px;background:var(--chart-3);color:#fff;border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">LR 불균형 (이중 회전)</button>
    <button id="avl-btn-rl" style="padding:6px 12px;background:var(--border);color:var(--foreground);border:none;border-radius:var(--radius);font-size:11px;font-weight:700;cursor:pointer">RL 불균형 (이중 회전)</button>
  </div>

  <!-- 회전 전/후 비교 카드 -->
  <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
    <div style="padding:12px;background:var(--background);border:1px solid var(--destructive);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--destructive);font-weight:800">회전 전 불균형 상태 (|BF| ≥ 2)</div>
      <div id="avl-view-before" style="font-size:12px;font-family:monospace;margin-top:6px;line-height:1.5">
        (30 [BF=+2])<br/>
        &nbsp;&nbsp;└── (20 [BF=+1])<br/>
        &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└── (10 [BF=0])
      </div>
    </div>
    <div style="padding:12px;background:var(--background);border:1px solid var(--chart-2);border-radius:var(--radius)">
      <div style="font-size:10px;color:var(--chart-2);font-weight:800">회전 후 균형 복원 상태 (|BF| ≤ 1)</div>
      <div id="avl-view-after" style="font-size:12px;font-family:monospace;margin-top:6px;line-height:1.5">
        &nbsp;&nbsp;&nbsp;&nbsp;(20 [BF=0])<br/>
        &nbsp;&nbsp;┌─────┴─────┐<br/>
        (10 [BF=0])&nbsp;&nbsp;(30 [BF=0])
      </div>
    </div>
  </div>
</div>

<script>
(function() {
  var bLL = document.getElementById('avl-btn-ll');
  var bRR = document.getElementById('avl-btn-rr');
  var bLR = document.getElementById('avl-btn-lr');
  var bRL = document.getElementById('avl-btn-rl');
  var vBef = document.getElementById('avl-view-before');
  var vAft = document.getElementById('avl-view-after');

  bLL.addEventListener('click', function() {
    vBef.innerHTML = '(30 [BF=+2])<br/>&nbsp;&nbsp;└── (20 [BF=+1])<br/>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└── (10 [BF=0])';
    vAft.innerHTML = '&nbsp;&nbsp;&nbsp;&nbsp;(20 [BF=0])<br/>&nbsp;&nbsp;┌─────┴─────┐<br/>(10 [BF=0])&nbsp;&nbsp;(30 [BF=0])';
  });

  bRR.addEventListener('click', function() {
    vBef.innerHTML = '(10 [BF=-2])<br/>&nbsp;&nbsp;└── (20 [BF=-1])<br/>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└── (30 [BF=0])';
    vAft.innerHTML = '&nbsp;&nbsp;&nbsp;&nbsp;(20 [BF=0])<br/>&nbsp;&nbsp;┌─────┴─────┐<br/>(10 [BF=0])&nbsp;&nbsp;(30 [BF=0])';
  });

  bLR.addEventListener('click', function() {
    vBef.innerHTML = '(30 [BF=+2])<br/>&nbsp;&nbsp;└── (10 [BF=-1])<br/>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└── (20 [BF=0])';
    vAft.innerHTML = '&nbsp;&nbsp;&nbsp;&nbsp;(20 [BF=0])<br/>&nbsp;&nbsp;┌─────┴─────┐<br/>(10 [BF=0])&nbsp;&nbsp;(30 [BF=0])';
  });

  bRL.addEventListener('click', function() {
    vBef.innerHTML = '(10 [BF=-2])<br/>&nbsp;&nbsp;└── (30 [BF=+1])<br/>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;└── (20 [BF=0])';
    vAft.innerHTML = '&nbsp;&nbsp;&nbsp;&nbsp;(20 [BF=0])<br/>&nbsp;&nbsp;┌─────┴─────┐<br/>(10 [BF=0])&nbsp;&nbsp;(30 [BF=0])';
  });
})();
</script>
```
